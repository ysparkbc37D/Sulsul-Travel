# 📜 술술체인지스 (Sulsul-Travel Changes & Version History)

본 문서는 **술술트래블 (Sulsul-Travel)**의 모든 버전별 변경 내역(신규 기능, 개선 사항, 버그 수정, 아키텍처 개편)을 엄격하게 기록하는 **공식 변경 이력서(CHANGELOG)**입니다.

모든 버전 번호는 [Semantic Versioning 2.0.0](https://semver.org/) 및 `술술트래블신록`의 버전 관리 원칙(`주.부.수`)을 엄격히 준수합니다.

---

## 📌 버전 관리 원칙 (Release Protocol: 술술 오도미터 순차 올림 원칙)
1. **버전 표기**: `v주.부.수` (예: `v1.0.0`)
2. **순차 올림 규칙 (Odometer Roll-over Model)**:
   - **제일 뒷자리부터 +1씩 전진**: 기능 추가, 버그 수정, 아키텍처 개편 등 릴리즈 규모와 무관하게 **모든 릴리즈는 항상 제일 뒷자리(`수`)부터 정확히 +1씩만 순차적으로 증가**합니다. 임의로 중간이나 앞자리를 건너뛰는 것을 금지합니다.
   - **자릿수 올림 (Carry-over at 9)**:
     - 제일 뒷자리(`수`)가 **9**에 도달한 다음 릴리즈에서만 바로 앞자리(`부`)를 `+1` 올리고 뒷자리(`수`)를 `0`으로 리셋합니다 (`v1.0.9` $\rightarrow$ `v1.1.0`).
     - 마찬가지로 중간 자리(`부`)가 **9**에 도달한 다음 릴리즈에서만 `주(Major)`를 `+1` 올리고 나머지를 `0`으로 리셋합니다 (`v1.9.9` $\rightarrow$ `v2.0.0`).
   - **순차 흐름 예시**: `v1.0.0` $\rightarrow$ `v1.0.1` $\rightarrow$ `v1.0.2` $\rightarrow$ ... $\rightarrow$ `v1.0.9` $\rightarrow$ `v1.1.0` $\rightarrow$ `v1.1.1`
3. **서비스 워커 캐시 갱신**:
   - 버전이 올라갈 때마다 `sw.js`의 `const V = 'st-shell-vX.Y.Z'`를 동일하게 갱신하여 클라이언트의 캐시 무효화를 보장합니다.
4. **버전 4중 동기화**:
   - `index.html`, `sw.js`, `CHANGELOG.md`, `술술트래블신록.md`의 최신 버전을 동일하게 유지하며 `tools-verify.ps1`로 검사합니다.

---

## [Unreleased]
### Planned
- [ ] 항공권/호텔 예약 바우처 PDF 자동 파싱 일정 등록
- [ ] 여행 동행자 실시간 음성 메모 AI 다이어리 자동 변환

## [v1.5.2] - 2026-09-12
### 💰 여행 가계부 UI 최적화: 아르헨 MEP 제거, 초기자금·총예산 완벽 일원화 & 전세계 통화 원터치 선택 모달 구축
- **가계부 메인 헤더 [🇦🇷 아르헨 MEP] 토글 스위치 제거 및 환율 관리 모달 이전**:
  - 메인 화면 가계부 상단에서 사용자 혼선을 유발하던 `[ 🇦🇷 아르헨 MEP ]` 토글 스위치를 완전히 제거하여 깨끗하고 직관적인 가계부 지표 뷰 제공.
  - 게이트키퍼(R-6, R-10: `chk-mep-rate`) 불변식을 100% 보존하기 위해, 해당 기능은 상단 환율 관리 모달(`modal-rates-manager`) 내 전용 고급 설정 카드로 이동 배치.
- **초기 현금 및 총 예산 설정 전용 모달 (`modal-initial-funds`) 구축**:
  - 기존의 단일 외화 텍스트 `prompt()`를 전격 폐지하고 바텀시트 전용 모달 신설.
  - 활성화된 통화별(KRW, USD, EUR, JPY 등) 보유 현금/자금을 실시간 환율과 함께 편리하게 입력.
  - 통화별 원화 환산액과 초기 총 자산이 실시간으로 합산 계산되며, `[초기 총 자산을 여행 총 예산으로 자동 적용]` 기본 옵션을 통해 첫 화면의 `총 예산`과 `남은 총 자산`이 완벽하게 일치하도록 일원화.
  - 메인 화면의 `[예산 변경]` 및 `[초기 현금·예산 설정]` 버튼 모두 이 통합 모달로 단일 연동.
- **전세계 68개국 여행 통화 지원 및 타이핑 없는 원터치 선택 모달 (`modal-add-currency`) 탑재**:
  - 기존 19개 한정 텍스트 타이핑 `prompt()`를 전면 퇴출.
  - 아시아, 유럽, 아메리카, 오세아니아, 중동/아프리카 대륙별 68개국 이상의 글로벌 여행 통화 레지스트리 구축.
  - 국문명(유로, 달러, 엔, 바트, 프랑 등), 영문 코드(EUR, USD, THB 등), 국가명(일본, 태국, 베트남 등) 실시간 검색 및 대륙별 필터 탭 제공.
  - 원하는 통화 카드를 터치하기만 하면 즉시 가계부 지갑이 생성되고 모달이 닫히는 원터치 UX 달성.
  - 초기 자금 설정 모달에서도 `[+ 새 통화 추가]`로 다이렉트 호출되어 신규 통화 추가 및 초기 자금 입력을 한 호흡으로 진행 가능.
- **실시간 환율 갱신 연동 & 환율 관리자 편의성 향상**:
  - 68개 전세계 통화가 `open.er-api.com` 실시간 API와 자동 연동되어 환율 갱신 지원.
  - 통화별 맞춤 환율 관리자(`modal-rates-manager`)에서 현재 여행에서 사용 중인 활성 통화가 최상단에 우선 정렬되도록 개선.

## [v1.5.1] - 2026-09-12
### 📱 모바일 화면 배치 깨짐(오버플로/좌측 잘림) 완벽 해결 및 여행 가계부 디자인·색감 전면 고급화
- **모바일 360px~430px 제로 오버플로(Zero-Overflow) 반응형 레이아웃 구축**:
  - `exp-form-amount` 금액 입력부 flex 컨테이너에 `min-w-0`를 적용하여 브라우저 기본 240px 너비 강제 고정 결함을 근절.
  - 우측 통화 셀렉트(`exp-form-curr`)를 `shrink-0 w-24 sm:w-28`로 안정화하여 360px 초소형 화면에서도 화면 안쪽 40px 안전 마진 내 100% 렌더링 보장.
  - 모달 컨테이너 `.sheet-box`에 `box-sizing: border-box; max-width: 100vw; overflow-x: hidden;` 가드를 적용하여 바텀 시트 좌측 타이틀('지출 기록 추가' $\rightarrow$ 'ㅣ출 기록 추가') 잘림 및 가로 스크롤 완전 해결.
  - 카테고리 10종 그리드를 `grid-cols-5 gap-y-3.5 gap-x-1 sm:gap-x-2`와 반응형 원형(모바일 44px, 데스크톱 48px)으로 배치하여 360px 기기에서도 완벽한 균등 정렬 구현.
- **술술트래블 시그니처 샴페인 골드 & 웜 린넨/옵시디언 럭셔리 에디토리얼 테마 전면 리디자인**:
  - **카테고리 원형 아이콘 (`.exp-cat-circle`) 테마 적응형 리디자인**:
    - 라이트 모드: 투박한 검은색 구멍을 제거하고 은은한 웜 샌드 아이보리 디스크(`background: #f8f6f0; border: 1.5px solid #e5e0d4`)로 전환, 선택 시 화사한 샴페인 골드 & 앰버 그라디언트 링(`linear-gradient(135deg, #fef3c7, #fde68a)`) 및 앰버 라벨(`#92400e`) 부각.
    - 다크 모드: 반투명 옵시디언 글래스 디스크 및 일렉트릭 샴페인 골드 글로우 링 적용.
  - **금액 & 통화 핀테크 럭셔리 카드 (`.exp-amount-card`)**:
    - 라이트 모드: 따뜻한 린넨 텍스처 그라디언트와 테두리 없는 대형 타이포그래피, 화이트 필 셀렉트 박스, 앰버 환율 배지(`≈ ₩0`).
    - 다크 모드: 딥 미드나이트 티타늄 그라디언트와 골드 환율 배지.
  - **애플/노션 스타일 소프트 세그먼트 컨트롤 (`.exp-pill-tray`, `.exp-pill-btn`)**:
    - `[카드 | 현금]`, `[지출 | 수입]`, `[간편입력 | 상세입력]`, `[지출구분 | 카테고리 | 수지]` 토글을 트레이 위에서 떠오르는 입체 화이트/네이비 카드로 현대화.
  - **트래블 컨시어지 AI 스캔 배너 (`.exp-scan-banner`)**:
    - 이질적이던 사이언 그린 대신 샴페인 골드 톤과 골드 스캔 버튼으로 우아한 여행 비서 느낌 연출.
  - **시맨틱 텍스트 & 인풋 클래스 (`.exp-heading`, `.exp-subtext`, `.exp-input`)**:
    - 라이트 모드(`--ink: #111827`, `--ink2: #374151`)와 다크 모드(`--ink: #ededed`, `--ink2: #a1a1aa`)에서 완벽한 가독성 보장.

## [v1.5.0] - 2026-09-12
### 📊 고도화된 여행 가계부(Travel Expense) UI/UX 전면 개편 & 통계 대시보드 구축
- **직관적인 다중 통화 지출 입력 인터페이스 (`#modal-expense`) 개편**:
  - `[간편입력]` (금액, 통화, 결제수단 카드/현금, 카테고리 1~2초 초고속 기록) vs `[상세입력]` (상세 메모, 결제 시간, 복수 사진 첨부) 상단 탭 분리 지원.
  - 상단 대형 결제 금액 헤더(`-15 USD ▼`) 및 실시간 인라인 원화(KRW) 환산 예상액(`≈ ₩20,177`) 자동 미리보기 제공.
  - 상단 듀얼 세그먼트 토글 탑재: `[💳 카드 | 💵 현금]` 결제수단 토글 및 `[🔴 지출 | 🟢 수입]` 거래구분 토글 지원.
  - 10대 시각적 원형 컬러 아이콘 카테고리 그리드(식사, 간식, 술, 음료, 숙소, 교통, 렌터카, 관광, 쇼핑, 기타) 완비.
- **미디어 첨부 및 대표 사진 지정 UX 탑재**:
  - 지출 등록 시 영수증, 음식, 현장 사진 복수 첨부 지원 (Canvas 최대 800px WebP/JPEG 자동 압축 저장).
  - 첨부 사진에 `[대표사진]` 배지 자동 부여 및 원터치 대표 사진 변경 기능 지원.
  - 지출 목록 카드(`renderExpensesList`)에 텍스트뿐만 아니라 52x52 대표 사진 썸네일 노출로 지출 기억 복기 지원.
- **Vision AI 기반 영수증/티켓 자동 입력 (Smart Scan) 연동 고도화**:
  - Gemini Vision AI OCR 판독 완료 시 상호명, 금액, 통화, 카테고리, 일자 자동 Pre-fill 및 시각적 하이라이트(그린 링) 효과 부여.
  - 스캔한 영수증 이미지를 지출 건의 대표 사진으로 100% 자동 첨부.
- **인터랙티브 시각화 통계 대시보드 (`#modal-expense-analytics`) 신설**:
  - 상단 총 지출 금액 볼드 노출 (`1,280,000 KRW`) 및 지출/수입 총 건수 요약 배너.
  - 순수 SVG 기반 인터랙티브 도넛 차트 렌더러 탑재 (중앙 지표: `지출 구분 85:15`, `카테고리 5가지` 등 터치 시 세부 확인).
  - 3대 분석 탭 완비: `[지출 구분]` (카드 vs 현금 비율), `[지출 카테고리]` (카테고리별 랭킹 및 점유율 %), `[지출 및 수입 구분]` (수지 현황 및 순지출).

## [v1.4.9] - 2026-09-11
### 💱 실시간 환율 갱신(Live FX Rate Refresh) & 통화별 맞춤 환율 관리 시스템 구축
- **원클릭 실시간 글로벌 시장 환율 갱신 엔진 (`open.er-api.com` API)**:
  - 가계부 헤더의 `[🔄 환율 갱신]` 버튼 클릭 시 18개국 전 세계 주요 통화의 최신 환율을 실시간으로 가져와 지갑 원화 환산 가치와 남은 총 자산을 즉시 동적 재계산.
  - 네트워크 단절이나 API 장애 상황에서도 앱이 중단되지 않고 로컬 캐시 환율 또는 내장 기본 환율로 안전하게 보호되는 그레이스풀 폴백(Graceful Fallback) 탑재.
- **마지막 갱신 시각 및 실시간 상태 안내 UI (`#exp-rates-timestamp`)**:
  - 가계부 헤더에 현재 적용 중인 환율 상태(기본 환율, 실시간 갱신 일시, 맞춤 설정 여부)를 직관적인 배지와 애니메이션으로 시각화.
- **통화별 맞춤 환율 관리 모달 (`#modal-rates-manager`) 신설**:
  - 여행자가 은행에서 주거래 우대 환율로 환전했거나 현지 사설 환전소 환율을 적용하고 싶을 때 직접 원하는 환율을 입력·저장할 수 있는 맞춤 관리 모달 제공.
  - 환율 산출 우선순위 계층(1순위: 아르헨티나 MEP 우대, 2순위: 사용자 맞춤 직접입력, 3순위: 실시간 API, 4순위: 기본 내장 환율)을 엄격히 준수.
- **환전 모달 실시간 환율 원터치 동기화 (`syncExchangeModalWithLiveRate`)**:
  - 환전 등록(`modal-exchange`) 화면에서 `[🔄 실시간 환율]` 버튼을 통해 현재 시장 기준 환율을 즉시 불러와 적용.

## [v1.4.8] - 2026-09-11
### 💱 여행 다통화 지갑(Multi-Currency Wallet) 및 유기적 환전·결제 가계부 관리 시스템 구축
- **다통화 지갑(Multi-Currency Wallet) 실시간 잔액 엔진 구축**:
  - 여행 중 원화(KRW), 달러(USD), 유로(EUR), 엔화(JPY), 위안(CNY) 등 다중 통화를 동시에 관리할 수 있는 통화별 지갑 시스템 구현.
  - 각 통화별 보유 잔액, 초기 자금, 환전 입출금, 지출 차감액을 실시간 동적 계산하여 데이터 왜곡 없이 완벽한 회계 정합성 보장.
  - 320px 모바일 완벽 수납 반응형 통화별 지갑 카드 그리드(`#exp-wallets-container`) 렌더링.
- **유기적 환전(Currency Exchange) 관리 모달(`modal-exchange`) 신설**:
  - 출금 통화(보내는 지갑)와 입금 통화(받는 지갑)를 선택하고 금액과 적용 환율을 입력하면 실시간으로 원화가 줄고 외화가 늘어나는 유기적 환전 엔진 구축.
  - 환전은 소비가 아닌 '자산 형태 전환'으로 취급되어 총 지출액을 왜곡하지 않고 총 여행 자산을 원형 보존.
  - 환전 취소/삭제 시 출금 통화와 입금 통화 잔액이 원상 복구되는 안전 롤백 메커니즘 탑재.
- **현지 지출 시 결제 수단/출금 지갑 스마트 연동(`modal-expense`)**:
  - 결제 통화 선택 시 `[현지 외화 지갑 잔액 차감]` vs `[한국 카드/원화(KRW) 결제 차감]` vs `[단순 기록(미차감)]` 선택 옵션 자동 연계.
  - 외화로 식사를 하더라도 한국 신용카드로 결제했으면 원화 통장에서 차감되고, 환전해 둔 외화 현금/트래블로그 잔액으로 결제했으면 외화 지갑 잔액에서 차감.
  - 지갑 잔액 부족 시 경고 안내 및 차감 후 잔액 실시간 프리뷰 배너 제공.
- **통화 간편 추가 & 출국 전 초기 외화 현금 자금 설정**:
  - 가계부 내 `[+ 통화 추가]`(`openAddCurrencyPrompt`)를 통해 18개국 통화 중 원하는 통화를 1초 만에 여행 지갑으로 활성화.
  - `[⚙️ 초기 현금 설정]`(`openAdjustInitialBalanceModal`)을 통해 여행 출발 전 이미 보유하고 있던 외화 현금을 손쉽게 초기 잔액으로 등록.

## [v1.4.7] - 2026-09-11
### 💳 여행 가계부 1급 탭 복원 및 다차원 자연 지출 업데이트 구축
- **메인 네비게이션 바(`companion-main-nav`) 5대 탭(가계부) 전격 복원**:
  - 기존 v1.3.0 개편 시 보조 메뉴로 축소되었던 `가계부`(`nav-tab-expenses`)를 1급 메인 탭(`큰 계획 · 오늘 · 가계부 · 기록 · 준비`)으로 전격 복원.
  - 320px~430px 초소형 모바일 화면에서도 `min-w-0 flex-1`, 52px 터치 타깃, 슬림 여백을 적용하여 5개 탭이 가로 스크롤이나 요소 겹침 없이 100% 완벽히 균등 배치되도록 최적화.
- **술술 통계 대시보드(`modal-analytics`) 즉시 연동 및 액션 버튼 신설**:
  - 통계 대시보드 내 `총 지출 / 목표 예산` 카드 터치 시 모달이 닫히며 즉시 가계부 탭으로 직행하는 상호작용(`closeModal; switchTab('expenses')`) 및 `[💳 가계부 바로가기 ➔]` 시각 힌트 제공.
  - 모달 하단 액션 바에 `[💳 가계부 관리]` 버튼을 신설하여 통계를 보며 곧바로 가계부로 전환 가능.
- **현장 "오늘"(`today`) 화면 1초 퀵 지출 버튼 탑재**:
  - `renderCompanionToday()`의 상단 및 `NEXT STOP` 액션 바에 `[💳 지출 기록]`(`openAddExpenseModal()`) 버튼을 추가하여 여행 중 커피/식사/교통비 결제 시 화면 이탈 없이 현장에서 즉시 1초 만에 지출 기록 가능.
- **가계부 탭 내 목표 예산 퀵 변경 & 전 세계 18개국 통화 자동 매칭**:
  - 가계부 화면 상단 `총 예산` 카드에 `[✏️ 예산 변경]`(`openEditBudgetPrompt`) 버튼을 탑재하여 전체 여행 설정을 열지 않고도 목표 예산을 즉시 수정 및 실시간 자동 반영.
  - 여행지 국가(스페인 $\rightarrow$ EUR, 윈난 $\rightarrow$ CNY, 남미 $\rightarrow$ USD 등)를 자동 감지하여 지출 등록 폼에 기본 결제 통화로 자동 매칭.
  - EUR, USD, JPY, CNY, GBP, CHF, ARS(MEP 우대 환율 연동), PEN, BRL, VND, THB 등 전 세계 주요 18개국 통화 실시간 원화 환산 딕셔너리 완비.

## [v1.4.6] - 2026-09-11
### 🤖 AI 일정 생성 타임아웃 확장 · JSON 모드 적용 및 스마트 로컬 엔진 폴백
- **Gemini AI 일정 생성 타임아웃 12초 하드 컷 중단 결함 완벽 해결**:
  - 기존 `callGeminiApiWithFallback`에 고정된 12초 타임아웃으로 인해, 다일정(5~22일 등) 일정 생성 시 API 응답 소요 시간(20~35초)을 견디지 못하고 3개 모델(`gemini-3.1-flash-lite`, `gemini-3.5-flash-lite`, `gemini-3.5-flash`)이 모두 `AbortError`로 중단되던 결함을 근절.
  - 생성 작업(`generateAiDraftPayload`) 전용으로 모델당 타임아웃을 30초(`timeoutMs: 30000`), 오케스트레이터 제한을 65초(`orchestratorTimeoutMs: 65000`)로 적응형 확장.
- **네이티브 JSON 모드(`response_mime_type: "application/json"`) 탑재**:
  - Gemini API 호출 시 `generationConfig`에 `response_mime_type: "application/json"`을 전달하여 모델이 순수 JSON만 반환하도록 강제.
  - 마크다운 코드 블록(```json ... ```) 및 후행 쉼표(trailing comma) 정돈을 지원하는 탄력적 JSON 파서(`parseJsonBlock`) 구축.
- **장기 여행 프롬프트 앵커 스팟 밀도 최적화 & 스팟 분 단위 자동 정규화**:
  - 8일 이상 장기 여행(예: 남미 22일 대종단) 시 하루 4~6개의 핵심 앵커 스팟으로 기획 밀도를 유연하게 조율하여 토큰 낭비 및 출력 단절을 예방.
  - AI가 `:15`, `:45` 등 비표준 분으로 응답하더라도 기각하지 않고 30분 단위(`:00`, `:30`)로 자동 반올림 정규화(`normalizeAiDraftDays`)하여 유효한 초안으로 보존.
- **무결점 스마트 로컬 엔진 자동 폴백(Smart Local Engine Fallback) 구축**:
  - API 키 유효성/할당량 초과, 타임아웃, 오프라인 네트워크 장애 등 어떠한 실패 환경에서도 사용자가 빈 화면이나 에러 토스트로 방치되지 않도록 즉시 내장 DKP 골든 코스 및 30분 일정 엔진(`generateAutoSpotsForTrip`)으로 무감청 전환.
  - AI 초안 검토 모달에 `스마트 로컬 추천 코스` 배너를 친절하게 안내하고 [일정 적용하기] 시 전체 일정이 100% 정상 저장되도록 보장.

## [v1.4.5] - 2026-09-11
### 📱 모바일 '+ 추가' 여행 생성창 가로 흔들림(Wobble) 근절 & 반응형 UI/UX 최적화
- **모바일 '+ 추가' 여행 생성 모달 가로 흔들림(Wobble/Jitter) 원천 박멸**:
  - 모바일 뷰포트(360px~390px)에서 '+ 추가'(`modal-create-trip`) 창 내부 스크롤 시 화면이 좌우로 미끄러지고 흔들리던 결함을 완벽 근절.
  - `.sheet-container .sheet-box.sheet-scroll-layout`, `#modal-create-trip .sheet-box`에 `overflow-x: hidden !important;`, `width: 100% !important;`, `max-width: min(680px, 100vw) !important;`, `box-sizing: border-box !important;` 격리 적용.
  - 모달 내부 스크롤 컨테이너(`.sheet-scroll-body`, `#modal-create-trip-scroll-body`)에 `overflow-x: hidden !important;`, `overscroll-behavior-x: none !important;`를 부여하여 대각선 스와이프에 의한 좌우 출렁임 완전 차단.
- **소도시 직접 추가 행 Flex 오버플로우 근절 & 콤팩트 규격화**:
  - 소도시 직접 추가 행의 `<input>`에 기본 너비 팽창을 방지하는 `min-w-0 flex-1 w-full` 수납 규칙 적용.
  - 모바일 환경에서 인풋 폰트 및 높이 강제 확장 현상을 보정하는 모달 전용 콤팩트 CSS 규칙(`min-height: 38px !important; font-size: 14px !important;`) 적용.
  - 플레이스홀더를 간결한 `"목록 외 소도시/근교 (예: 론다)"`로 최적화하여 360px 디바이스 가용 폭 내에 `[입력창] + [+ 도시 추가]` 버튼이 우측 잘림 없이 완벽 수납되도록 개선 (63px 오버플로우 $\rightarrow$ 0px 소멸).
- **목적지 보드 카드 및 헤더/배너 반응형 패딩 최적화**:
  - 모바일 3중 중첩 패딩(40px+28px+24px)을 12px(`0.75rem`)로 슬림화하여 360px 화면 기준 유효 작업 너비를 268px에서 336px(+68px, 25%↑)로 쾌적하게 확장.
  - 목적지 보드 카드(`#destination-board-cards`) 및 시차 안내 배너(`#trip-form-tz-banner`)에 `w-full max-w-full overflow-hidden`과 `flex-col sm:flex-row gap-1`을 적용하여 140px 오버플로우 원천 해소.
  - 대륙 탭 래퍼 및 통화/예산 행에 `truncate min-w-0`을 적용하여 가로 스크롤 발생 0건(`sBodyHasHScroll: false`) 달성.

## [v1.4.4] - 2026-09-11
### 🌏 국가 필터 내 도시 노출 결함 근절 & 지명 자가 치유(Self-Healing) 엔진 탑재
- **국가 필터 드롭다운 내 도시/성 오노출 결함 근절**:
  - 모바일 국가 선택 시트(`hub-filter-country`)에 국가가 아닌 도시/성 단위 지명(`다리`, `리장 (CNY)`, `샹그리아`, `운남 (CNY)`, `중국 연태 (CNY)`, `칭다오` 등)이 별도 국가로 노출되던 문제를 근본적으로 해결.
  - `updateHubFilterDropdowns()`가 오직 정규화된 표준 국가명(`COUNTRY_DATABASE`의 `canonicalName`)만 수집하도록 개편하여 도시가 국가 목록에 오염되는 현상을 원천 차단.
- **표준 국가 변환 엔진 (`resolveCanonicalCountry`) 및 도시 DB 대폭 확충**:
  - 임의의 지명/도시명/복합지명(`중국 연태`, `리장`, `다리`, `운남`, `칭다오` 등)이 입력되어도 상위 표준 국가(`중국`)와 소속 도시(`연태`, `리장` 등)를 정확히 분리·해석하는 전용 엔진 구현.
  - 중국 항목에 `다리`, `따리`, `리장`, `샹그릴라`, `샹그리아`, `운남`, `윈난`, `칭다오`, `청도`, `연태`, `옌타이`, `호도협`, `석림` 등 30여 개 대표 여행지를 전수 보강하고, 일본/베트남/미국/유럽 주요 도시 별칭 확충.
- **여행 데이터 자동 자가 치유(Self-Healing Migration) 탑재**:
  - 앱 초기화(`initApp()`) 시 사용자의 기존 로컬 스토리지 데이터를 전수 순회하여 `trip.countries` 내 도시/성 지명을 표준 국가로 승격하고 원래 도시는 `trip.cities`로 안전하게 보존·이전. 사용자의 기존 데이터가 즉시 깨끗하게 정상화됨.
- **허브 필터·카드 뱃지·통계 카운트 정합성 완벽 정상화**:
  - `renderTripCards()`에서 국가 필터(`중국`) 선택 시 소속 도시 여행이 누락 없이 100% 매칭되도록 보정.
  - 여행 카드에 `[🇨🇳 중국]`(국가 뱃지)과 `[📍리장]`(도시 뱃지)이 명확히 구분 표시되도록 분리하여 도시명 중복 및 국기 부조리 해소.
  - 워크스페이스 상단 및 통계 대시보드에서 `1개국 3도시`로 정확하게 집계되도록 보정.

## [v1.4.3] - 2026-09-11
### 🏷️ 모바일 헤더 버전 뱃지 상시 노출 복원 & 릴리즈 히스토리 최신화
- **모바일 헤더 버전 뱃지 상시 노출 복원**:
  - `css/companion.css`의 모바일 미디어쿼리(`@media (max-width: 767px)`)에서 `#app-ver-badge`가 `display: none`으로 숨겨져 사용자가 앱 버전 반영 여부를 알 수 없던 문제를 해결.
  - `#app-ver-badge`를 `display: inline-flex !important`로 지정하여 스마트폰 화면에서도 'Sulsul Travel' 브랜드 옆에 버전 뱃지가 항상 선명하게 표시되고, 클릭 시 버전 히스토리 모달이 즉시 열리도록 복원.
- **버전 릴리즈 히스토리 뷰어 최신화**:
  - `modal-version-history`에 누락되었던 `v1.4.3`, `v1.4.2`, `v1.4.1` 최신 릴리즈 카드를 전격 추가하고 모달 푸터 엔진 버전을 `v1.4.3`으로 갱신.
  - 사용자가 버전 뱃지나 설정 모달을 통해 모든 업데이트 내역을 한눈에 확인할 수 있도록 정보 투명성 확보.

## [v1.4.2] - 2026-09-11
### 🧹 상단 헤더 불필요 아이콘 완전 정돈 · 모바일 모달 흔들림 원천 박멸 · 여행 추가창 클린 선택기 고도화
- **상단 헤더 불필요/깨짐 아이콘 전면 제거 및 슬림 미니멀 정돈**:
  - 모바일 환경에서 텍스트 없이 빈 초록 사각형 박스로 깨져 보이던 `#ai-status-indicator`를 상단 헤더에서 완전히 제거하고, 환경설정 모달의 AI 탭(`panel-settings-ai`) 내부로 깔끔히 이전.
  - 상단 헤더의 불필요한 AI 비서(요술봉) 버튼을 정리하여 좌측 [Sulsul Travel 브랜드], 우측 [⚙️ 설정], [+ 추가] 2대 핵심 액션만 남겨 모바일 가로 공간 및 시각적 안정감 극대화.
- **모바일 환경설정 모달(AI·동기화 탭) 및 여행 추가창 흔들림(Jitter) 원천 차단**:
  - `css/companion.css`의 모바일 미디어쿼리(`@media (max-width: 767px)`) 내 `.sheet-scroll-layout`, `#modal-settings .sheet-box`, `#modal-create-trip .sheet-box`에 고정 높이 락(`height: min(86dvh, ...) !important; overflow: hidden !important;`)과 독립 스크롤 바디(`min-height: 0 !important; overflow-y: auto !important;`)를 강제 주입.
  - 탭 전환(`화면·테마` ↔ `AI·동기화`) 및 아코디언 토글 시 모달 박스 전체가 들썩거리던 레이아웃 변형을 100% 제거.
- **새 여행 추가창 클린 목적지 선택기 개선**:
  - 창 오픈 시 특정 국가(스페인)가 자동 강제 선택되던 프리셋을 제거하여 깨끗한 상태에서 원하는 국가를 선택하도록 개선.
  - 국가 선택 시 대표 도시 자동 선택을 배제하고, 사용자가 실제 방문할 도시를 순서대로 클릭하여 `①`, `②`, `③` 순번 뱃지를 붙이도록 직관성 강화.
  - 폼을 수정하지 않고 닫을 때 불필요하게 뜨던 이탈 경고 팝업 차단.

## [v1.4.1] - 2026-09-11
### 🌍 전 세계 국가·도시 스마트 필터 & 복수 선택 보드 탑재 · 설정창 흔들림 완벽 해소 · 새 여행 등록창 UI/UX 전면 개편
- **환경설정 모달(AI·동기화 탭) 화면 흔들림(Jitter) 100% 완전 박멸**:
  - `.sheet-scroll-layout` 표준 3단 아키텍처 적용 (`sheet-sticky-header` 고정 + `sheet-scroll-body` 독립 스크롤 `min-height: 0` + 모바일 안정 높이 락 `height: min(86dvh, ...)` + `scrollbar-gutter: stable` + `overscroll-behavior-y: contain` + `touch-action: pan-y`).
  - 아코디언 토글, 탭 전환, 터치 스크롤 시 화면 전체가 출렁이거나 덜컹거리던 레이아웃 스래싱(Layout Thrashing)을 완벽 차단.
- **새 여행 등록창(`modal-create-trip`) UI/UX 전면 개편**:
  - `.sheet-scroll-layout` 적용으로 상단 헤더와 하단 `[새 여행 등록하기]` 버튼을 뷰포트에 상시 고정.
  - 끝없이 길었던 단일 스크롤 입력을 `어디로 떠나시나요?(목적지·도시)`, `언제 떠나시나요?(일정·시간)`, `어떤 여행을 원하시나요?(스타일·AI설계)` 3대 감성 카드 섹션으로 정갈하게 분리.
- **전 세계 국가·도시 스마트 필터 & 국가별 방문 도시 복수 선택 시스템 구축**:
  - 전 세계 8개 권역 80+ 국가 및 400+ 대표 여행 도시 메타데이터를 망라하는 확장 `COUNTRY_DATABASE` 완성.
  - 대륙 탭(`전체`, `동아시아`, `동남아`, `유럽`, `미주`, `대양주·중동`)과 초성/한글/영문 실시간 검색창으로 전 세계 모든 국가를 즉각 탐색.
  - 국가 선택 시 해당 국가 전용 **방문 도시 멀티 칩 보드**가 펼쳐져 복수 도시 선택 시 `① 마드리드`, `② 세비야`, `③ 바르셀로나` 순번이 매겨져 지리적 동선 체계화.
  - 목록에 없는 소도시나 근교 마을도 즉시 등록할 수 있는 `+ 도시 추가` 인풋 지원.
  - 다국가 여행(예: 스페인 + 포르투갈) 시 국가별로 도시가 묶여 분리되고, 통화 및 시차/비행시간이 자동으로 완벽 연동.
  - 체계화된 방문 순서와 국가별 도시 메타데이터를 AI 생성 파이프라인에 전달하여 30분 단위 초밀도 추천 일정을 오차 없이 완성.

## [v1.4.0] - 2026-09-11
### 🏷️ 활성 필터 요약 칩 · AI 신호등 인디케이터 · 국가 국기 및 통화 비주얼 전면 강화
- **실시간 활성 필터 요약 칩 (`#hub-active-chips`)**: 상태, 검색어, 국가(국기 포함), 도시, 컨셉, 정렬 조건 활성화 시 감성 태그 칩 노출 및 `✕` 원클릭 개별 해제와 전체 초기화 지원.
- **AI 연결 상태 신호등 인디케이터 (`#ai-status-indicator`)**: 앱 상단 헤더 및 AI 설정창에 Gemini 연결 상태(🟢 정상 / 🟡 Fallback / 🔴 키 필요)를 실시간 반영하고 원클릭 AI 설정 탭 포커싱 제공.
- **전 세계 50+ 국가·통화 마스터 데이터베이스 (`COUNTRY_DATABASE`)**: 사용자 참조 전 세계 여행지 리스트 기반으로 국기 이모지, ISO 4217 통화 코드, 한글명, 주요 거점 매핑 데이터 구축.
- **여행 카드 및 국가 드롭다운 비주얼 혁신**: 모든 여행 카드에 국가 국기, 통화 뱃지(`[💶 EUR]` 등), 거점 도시 태그를 추가해 여권/보딩패스 감성 극대화.
- **새 여행 등록 모달 스마트 연동**: 방문 국가 선택/입력 시 기준 통화 자동 매칭 및 인기 여행지 원클릭 프리셋 지원.

## [v1.3.9] - 2026-09-11
### 🔍 허브 4대 필터 개편 & 설정창 UI/UX 전면 정돈
- **국가·도시·컨셉·필터 4대 분류 체계**: 여행 허브에 도시 필터를 정식 도입하고, 국가 선택 시 해당 국가의 도시만 동적 연동하는 스마트 필터링 구현.
- **모바일 2x2 그리드 레이아웃**: 모바일에서 텍스트 잘림과 터치 오류를 방지하도록 `grid-cols-2 sm:grid-cols-4`로 재배치하여 44px 이상의 쾌적한 터치 영역 확보.
- **설정 모달 3대 카테고리 탭 시스템**: 끝없이 길었던 설정창을 `🎨 화면·테마`, `🤖 AI·동기화`, `💾 데이터·관리` 3대 탭으로 분류해 모바일 시스템 설정 수준의 정갈한 정보 구조 확립.
- **화면 흔들림(Jitter) 근본 해결**: CSS 음수 마진/sticky 헤더 충돌을 제거하고 헤더 고정 + 본문 독립 스크롤(Flexbox)로 모달 렌더링 흔들림 100% 해소.

## [v1.3.8] - 2026-09-11
### 휴대폰을 위한 여행 노트
- 밝은 회녹색·흰색 바탕과 초록색 행동 버튼으로 개편하고 큰 계획·오늘·기록·준비 메뉴를 모바일 하단에 고정.
- 거점별 여정과 전체 시간표를 분리하고 도시 카드에서 큰 계획 편집·구간 AI 재생성으로 연결.
- 상세 날짜 선택, 선택일에 일정 추가, 완료 일정 접기, 다음 일정과 순간 기록 중심의 오늘 화면 적용.
- 44px 이상 핵심 터치 영역과 14~17px 일정 본문, 입력창 16px, 기기 안전 영역·키보드 대응 보완.
- 설치 팝업 자동 표시를 제거하고 기록·준비 화면의 문구와 편집 동작을 정리.
- Tailwind CSS를 사전 생성해 제공. 기존 아이콘·Leaflet·LZ-String도 고정 버전의 로컬 자산으로 제공해 CDN 단절 시 화면 붕괴를 방지.
- 외부 요청 차단 상태의 브라우저에서 320~1280px와 날짜별 편집·완료·뒤로가기·메뉴 전환 검증.

## [v1.3.7] - 2026-09-11
### 🗺️ 도시·큰 계획 정렬과 30분 일정 밀도 보정
1. **도시·일차 제목 적용**: AI 승인 시 스팟뿐 아니라 각 Day의 도시와 제목도 반영해 최초 도시명이 모든 카드에 남는 문제를 수정.
2. **큰 계획 재그룹화**: 최초 AI 생성과 기존 자동 초안의 승인 시 임시 `planBlockId`를 지우고 승인된 도시의 연속 구간 기준으로 큰 계획 카드를 다시 구성.
3. **일차 밀림 차단**: 응답 일수와 `dayNum` 순서를 입력 기간과 대조하고 사용자가 정한 날짜는 AI 응답으로 덮어쓰지 않도록 보호.
4. **일정 밀도·시간 검증**: 일반일 8~12개, 첫날·마지막 날 5~8개 일정을 요청하고 최소 밀도와 `:00`/`:30` 시간 규칙을 통과한 초안만 검토 화면에 표시.
5. **자동 재시도**: 일수·순서·밀도·시간 형식이 부족한 응답은 최대 3회까지 다시 생성한 뒤에도 실패하면 원본을 유지.

## [v1.3.6] - 2026-09-10
### ✏️ 큰 계획 편집과 AI 재생성 연결
1. **저장 후 AI 재생성**: 큰 계획 편집 화면에 해당 카드만 바로 다시 설계하는 행동을 추가하고 AI 제안 → 비교 → 승인 흐름으로 연결.
2. **편집 조건 우선 반영**: 카드 제목, 거점, 희망 기간, 숙소, 우선 경험, 위험·예약 메모를 선택 구간 AI 프롬프트에 전달.
3. **카드 행동 명확화**: 큰 계획 카드에 `세부 계획`, `편집`, `AI 재생성`을 글자로 표시해 모바일에서도 기능을 바로 찾도록 개선.
4. **실패 시 원본 보존**: API 키 누락, 오프라인, AI 실패 시 저장한 큰 계획과 기존 세부 일정을 유지하고 같은 카드에서 재시도 가능하도록 안내.
5. **자유여행 강제값 제거**: 국가나 테마가 비어 있을 때 `자유여행`을 임의 저장하지 않고 입력된 국가·도시·스타일을 우선 사용.

## [v1.3.5] - 2026-09-10
### 🤖 주요 도시 기반 AI 여행 생성 연결
1. **AI 자동 설계 실제 호출**: 새 여행 생성의 `AI 맞춤 일정` 체크 시 로컬 샘플 일정으로 대체하지 않고 Gemini 초안 생성 → 검토 → 승인 흐름을 실행.
2. **주요 도시 입력 추가**: 국가/지역과 분리된 도시·거점 입력을 추가하고 입력 순서를 AI 프롬프트와 초기 거점에 전달.
3. **국가·도시 파싱 개선**: 괄호 안 도시 목록과 쉼표 목록을 안전하게 분리해 `자유여행` 기본값으로 잘못 떨어지는 경로를 줄임.

## [v1.3.4] - 2026-09-10
### 💾 AI 여행기 저장 안정화
1. **저장 공간 초과 대응**: 사진이 많은 모바일 여행에서 localStorage가 가득 차도 승인한 AI 여행기 문장을 IndexedDB 보조 저장소에 보관.
2. **자동 복구**: 앱 재실행 시 보조 저장된 여행기 내용을 원래 여행 카드에 병합.
3. **원본 보호**: 기존 여행 데이터가 저장 실패로 덮어써지지 않도록 보조 저장 성공 후에만 적용 상태를 완료.

## [v1.3.3] - 2026-09-10
### 🔧 신규 사용자 무료 Gemini 연결
1. **무료 티어 우선 모델**: 신규 프로젝트 접근이 제한된 `gemini-2.5-pro` 대신 무료 티어가 명시된 `gemini-3.1-flash-lite`를 1순위로 사용.
2. **안정 폴백**: `gemini-3.5-flash-lite` → `gemini-3.5-flash` 순서로 재시도해 모델별 접근 제한과 일시 오류에 대응.
3. **상태 문구 정정**: AI 비서의 오래된 `Gemini 2.0 Flash 연동됨` 표시를 `Gemini 3.1 Flash-Lite 연동됨`으로 갱신.

## [v1.3.2] - 2026-09-10
### 🔧 Gemini 연결 안정화
1. **최신 모델 폴백**: 종료된 `gemini-2.0-flash`·`gemini-1.5-flash` 대신 `gemini-2.5-flash` → `gemini-2.5-flash-lite` → `gemini-2.5-pro` 순서로 호출.
2. **API 키 연결 테스트 동기화**: 설정 화면의 연결 확인이 실제 폴백 목록의 첫 모델을 사용하도록 수정.
3. **키 전송 방식 개선**: Gemini 요청에서 API 키를 URL 쿼리 대신 `x-goog-api-key` 헤더로 전달.

## [v1.3.1] - 2026-09-10
### ✏️ 큰 계획 구간 편집·범위 RePlan과 모바일 복귀 UX
1. **큰 계획 구간 상세 화면**: 큰 계획 카드를 누르면 고정 헤더와 하단 액션을 갖춘 모바일 상세 화면에서 해당 구간의 날짜별 일정을 확인·수정.
2. **큰 계획 편집**: 카드 제목, 거점, 시작·종료일, 숙소 기준, 우선 경험, 위험·예약 메모, 합의 상태를 `planBlockMeta`에 저장하고 기존 일정 데이터와 하위 호환 유지.
3. **선택 구간 RePlan**: `range:start:end` 범위를 일정 재조정 도메인에 추가하고, 구간 상세에서 해당 카드의 날짜만 즉시 재배치하거나 AI 초안으로 재생성.
4. **변경 범위 보호**: 완료·고정 스팟 보호와 함께 선택 구간 밖 날짜의 불변을 검증한 뒤 승인된 초안만 revision 경계를 통해 적용.
5. **완료 일정 보기**: 구간 상세의 예정 일정과 완료 일정을 분리하고 완료 카드는 접어서 현재 계획에 집중하면서 필요할 때 기록을 확인.
6. **모바일 뒤로가기**: History API를 연결해 휴대폰 뒤로가기 시 열린 모달을 먼저 닫고, 다음 뒤로가기에서 큰 계획 목록과 이전 스크롤 위치로 복귀.
7. **안전 영역과 고정 조작부**: `safe-area-inset-*`와 `100dvh`를 적용하고 일정 튜닝·검토·큰 계획 편집 창의 헤더와 하단 버튼을 고정. 접근성 확대를 막던 viewport 제한도 제거.

## [v1.3.0] - 2026-09-10
### 🧭 큰 계획에서 현장 RePlan과 여행 기록까지 이어지는 동반자 UI
1. **여행 단계 중심 4개 메뉴**: 워크스페이스를 `큰 계획 / 오늘 / 기록 / 준비`로 정리하고 기존 가계부·동행·백업은 관련 화면에서 계속 접근할 수 있도록 배치.
2. **거점 중심 큰 계획 카드**: 연속된 일정을 도시·거점 단위로 묶고 기간, 대표 경험, 완료·고정 보호 개수를 한눈에 확인한 뒤 세부 시간표로 이동.
3. **현장용 오늘 화면**: 다음 행동, 오늘 진행률, 방문 확인, 건너뜀, 순간 기록, 지도, RePlan을 모바일 우선 화면에 결합.
4. **승인형 RePlan**: 완료·고정 일정을 제외한 변경 전후를 비교하고 승인해야만 적용. 승인 전 원본 변경, 중복 적용, 저장 실패를 방지하는 revision 기반 저장 경계 적용.
5. **AI RePlan 보호 계약**: AI 입력에 현재 일정과 완료·고정 제약을 전달하고 결과 검증을 통과한 초안만 검토 화면에 노출. 오프라인·실패 시에도 휴리스틱 대안을 자동 적용하지 않고 별도 승인 화면으로 전환.
6. **세부 일정 편집 통합**: 기존 미커밋 작업의 스팟 수정·팁·고정 토글을 유지하고 RePlan 보호 규칙과 연결.
7. **캐시 격리**: 서비스 워커가 `st-shell-` 접두사의 이전 술술트래블 캐시만 정리하도록 범위를 제한.
8. **실사용 시간 형식 지원**: `08:00 - 11:00`, `21:00 ~`, 자정 넘김을 포함한 일정 시간을 RePlan 변경안에서 올바르게 이동.
9. **공유·보관 표현 정비**: 여행 사본 링크, 기기 저장, 선택적 Gist 백업의 경계를 명확히 하고 실시간 공동편집·영구 보관으로 오인될 문구와 가짜 룸코드 복제를 제거.

## [v1.2.9] - 2026-09-07
### 📱 모바일 뷰포트 레이아웃 정밀 개선 & 타이포그래피 줄내림·줄바꿈 방지 및 GPS 레이더 버그 완치
1. **글로벌 CJK 타이포그래피 안전 규격 (`word-break: keep-all; overflow-wrap: break-word;`) 일괄 적용**:
   - 모바일 실기기(360px~390px 좁은 뷰포트)에서 한글 음절 단위 끊김 현상(예: '포토일기'가 '포토일\n기'로 쪼개지거나, '시간표'가 '시\n간표'로 깨지는 왜곡)을 앱 전체 `body` 및 모든 인터랙티브 요소(`button`, `nav`, `.tab-title`)에 걸쳐 원천 차단.
2. **워크스페이스 메인 5대 탭 및 4대 서브탭 반응형 완벽 보존**:
   - 메인 탭 5종(`일정/지도`, `가계부`, `포토일기`, `준비물`, `공유/설정`) 및 서브탭 4종에 `shrink-0 whitespace-nowrap`을 일괄 적용.
   - 탭 전환 함수(`switchTab`, `switchPlanSubTab`) 실행 시 동적으로 `className`이 재할당되더라도 `shrink-0 whitespace-nowrap`이 절대 유실되지 않도록 견고하게 개선. 부드러운 가로 스크롤(`overflow-x-auto no-scrollbar`)과 완벽 결합.
3. **GPS 스팟 레이더 주변 랭킹 `undefined` 리터럴 렌더링 버그 근본 패치**:
   - 스팟 데이터에 `flag` 속성이 없을 때 JavaScript 템플릿 리터럴에서 `"undefined "` 문자열이 렌더링되던 버그 완치 (`s.icon || s.flag || '📍'`).
   - `pack-default.js` 스팟 생성 시에도 기본 `flag`를 안전하게 세팅.
   - 주변 스팟 알약(Pill) 클릭 시 해당 스팟 위치로 GPS 모의 시뮬레이션을 즉시 전환하는 인터랙티브 편의 기능 탑재.
4. **GPS 스팟 레이더 컨트롤 및 액션 버튼 모바일 반응형 정렬**:
   - 모의 GPS 선택 드롭다운과 `[내 위치 탐색]` 버튼을 모바일 1줄 가로 배치(`flex-1 min-w-0` + `shrink-0 whitespace-nowrap`)로 재정돈.
   - 스팟 카드 내 `[+ 오늘 일정에 추가]` 및 `[길찾기]` 버튼의 비대칭 세로 쏠림 현상을 가로 50:50 균등 배치로 정돈.
   - 상단 액션 바 및 일정 튜닝 모달 내 긴 버튼명을 모바일 해상도에 맞게 반응형으로 간소화(`[일정 튜닝]`, `[즉시 재배치]`, `[AI 재생성]`).

## [v1.2.8] - 2026-09-07
### 🌍 국가별 시차(Time Zone Offset) & 비행시간 스마트 자동 연동 엔진
1. **국가별 시차 및 비행 소요 시간 오프라인 매트릭스 (`detectDestinationTimezone`)**:
   - 일본/동아시아(시차 0h, 비행 2.5h), 중화권/대만/몽골(시차 -1h, 비행 3.5h), 동남아(시차 -2h, 비행 5h), 유럽 전역(시차 -7~8h, 비행 14h), 미 서부(시차 -16h, 비행 11.5h, 날짜변경선), 미 동부(시차 -13h, 비행 14.5h), 하와이(시차 -19h), 대양주(시차 +1h), 남미(시차 -14h) 등 12대 권역 시차 및 평균 비행소요시간(직항/경유) 내장 매트릭스 구축.
   - 외부 네트워크나 백엔드 없이도 100% 오프라인에서 여행지 입력 즉시 지능적 시차 및 비행시간 판별.
2. **출발지(한국 KST) vs 여행지(현지시각) 이원화 스케줄 역산**:
   - 사용자가 입력한 '현지 도착 시간'을 바탕으로, 시차와 비행소요시간을 정밀 역산하여 한국 시간 기준 집 출발, 인천공항 수속, 비행기 이륙 시간을 계산.
   - 타임라인 스팟 카드에 시간대(KST vs 현지시각)를 명시하여 현실 비행 스케줄과 완벽 일치.
3. **귀국편 '+1일(익일 도착)' 날짜 래핑 및 전용 뱃지**:
   - 장거리(유럽, 미주 등) 귀국 비행 시 한국 공항 도착 및 집 귀가 일정을 `+1일 익일` 뱃지 및 시간대로 표현하여 실제 날짜 넘어감 현상 정밀 반영.
   - 시간 연산 유틸리티(`timeToMinutes`, `minutesToTime`)에 `+1일` 인식 및 정렬 안정화 탑재.
4. **여행 등록 모달 실시간 스마트 안내 뱃지 (`#trip-form-tz-banner`)**:
   - 여행지명 또는 방문 국가 입력 시 시차 안내 및 예상 비행시간이 실시간 인터랙티브 뱃지로 자동 갱신.

## [v1.2.7] - 2026-09-07
### ⚡ 교통 이용시간 기반 실전 초안 생성 및 실시간 변수 대응 일정 튜닝 엔진
1. **교통 이용시간(도착/종료) 기반 실전 라이프사이클 초안 자동 생성**:
   - 새 여행 등록 모달(`modal-create-trip`)에 `현지 도착 시간`(`trip-form-arrival-time`, 기본 14:00) 및 `현지 종료(비행) 시간`(`trip-form-departure-time`, 기본 17:00) 입력 필드 및 원터치 시간 칩 탑재.
   - `generateAutoSpotsForTrip` 고도화:
     - **Day 1 (도착 라이프사이클)**: 집 출발 ➔ 공항 이동 ➔ 출국 수속/보안검색 ➔ 비행 이동 ➔ 현지 공항 착륙(도착시간) ➔ 숙소/호텔 이동 ➔ 체크인 & 짐 정리 ➔ 첫날 시그니처 디너 & 나이트워크 자동 연계.
     - **Day N (귀국 라이프사이클)**: 호텔 체크아웃 & 짐 보관 ➔ 기념품 쇼핑/마켓 ➔ 라스트 런치 ➔ 숙소 짐 픽업 & 공항 이동 ➔ 출국 수속 & 면세점 ➔ 비행기 탑승(종료시간) ➔ 귀국 입국 심사 ➔ 집 도착 짐 정리 및 휴식 완벽 지원.
     - **Day 2 ~ Day N-1**: 30분~1시간 단위 최적화된 테마별 관광, 미식, 카페 투어 일정 자동 구성.
2. **실시간 현장 변수 대응 및 AI 일정 재조정/튜닝 엔진 (`modal-schedule-tuning`)**:
   - 항공편/열차 연착(`+60분`), 극심한 교통 체증(`+120분`), 우천/기상 악화(실내 코스 전환), 체력 저하/힐링 휴식(`+90분`), 스팟 조기 관람(`-60분`) 등 원터치 현장 변수 프리셋 탑재.
   - 오프라인 즉시 일정 재배치(1초 미만 휴리스틱) 및 AI 스마트 재생성(오케스트레이터 및 `modal-ai-draft-review` 초안 검토 연계) 듀얼 모드 지원.
   - 타임라인 상단 액션 바 및 각 Day별 아코디언 헤더에 `[⚡ 일정 튜닝]` 버튼을 상시 배치하여 손쉬운 접근성 보장.
3. **방문 완료 스팟 불변 보호 원칙 및 원클릭 방문 체크**:
   - 타임라인 각 스팟 카드에 `[✔️ 방문 완료]` 체크 토글 버튼 제공.
   - 불변 원칙: 이미 방문 완료(`completed === true`) 처리된 스팟은 일정 튜닝 시 시간을 절대 변경하지 않고 고정 보존하며, 아직 방문하지 않은 미래 일정만 안전하게 순차 리밸런싱.
4. **글로벌 API 및 오프라인 회복력 강화**:
   - `window.SulsulTravel`에 `openScheduleTuningModal`, `executeScheduleTuning`, `rebalanceTripSchedule`, `toggleSpotCompleted` 전격 노출.

## [v1.2.6] - 2026-09-07
### 🛠️ 새 여행 등록 엔진 ReferenceError 픽스 및 전 세계 맞춤 여정 안정화
1. **새 여행 등록 버튼 먹통 긴급 해결**:
   - `submitCreateTrip()` 내부에서 미선언 변수(`isYN`)를 참조하여 발생하던 치명적 `ReferenceError` 버그 픽스.
   - 여행 등록 및 워크스페이스 전환 전 과정을 견고한 `try ... catch` 블록으로 방어하여 예외 발생 시에도 원인을 안내하도록 조치.
2. **전 세계 맞춤 여정 입력값 지능형 정규화**:
   - 국가/지역명 입력 시 괄호 표기(예: `몽골(울란바토르, 카라코룸...)`)를 스마트 분리하여 거점 대표명과 상세 도시 목록을 동시 추출.
   - 버킷리스트와 방문 국가로부터 도시 목록(`trip.cities`)을 자동 구성하여 워크스페이스 거점 수 통계와 DKP 퀵 버튼에 완벽 반영.
3. **목적지 국기 및 테마 자동 매칭**:
   - 몽골(`🇲🇳`), 일본(`🇯🇵`), 미국(`🇺🇸`), 프랑스(`🇫🇷`), 스페인(`🇪🇸`), 베트남(`🇻🇳`), 호주(`🇦🇺`) 등 전 세계 대표 목적지 국기 이모지 자동 매칭.
   - 몽골 전용 `preset-mongolia` 푸른 초원 딥블루 테마 프리셋 추가.
4. **일반 여행 전용 고품질 사전 체크리스트 기본 탑재**:
   - 중국 운남/남미 외 신규 맞춤 여행 등록 시 여권 유효기간, E-티켓, 바우처, 트래블로그/월렛 카드, 여행자 보험, 로밍/eSIM, 비상 상비약, 멀티 어댑터 등 필수 8대 체크리스트 자동 생성.

## [v1.2.5] - 2026-09-07
### 🖼️ 허브 카드 지도 중복 제거 & 대표 사진·히어로 카드 계층화
1. **지도 정보 중복 100% 제거**:
   - 카드 상단 우측 액션 버튼에서 중복 배치되었던 `[🌐]` 지도 아이콘을 제거하고, 하단 `[🗺️ 경로 지도]` 버튼으로 단일화하여 직관성 극대화.
2. **여행 대표 사진(Cover Photo) 전용 공간 및 정규화 엔진**:
   - 모든 여행에 남미 지도가 깔리던 획일적 배경 버그를 제거.
   - 16:9 와이드 비율 중앙 크롭 및 자동 압축 정규화(800x450, JPEG 0.82) 기반 `[📷 대표 사진]` 등록/변경 및 기본 테마 복원 기능 구현.
   - 다이어리에 등록된 여행 사진이 있을 경우 첫 번째 사진을 자동으로 대표 커버 후보로 연동.
   - 미등록 시 여행지별 맞춤 테마 프리셋(운남: 에메랄드 산악 그라디언트, 남미: 앰버 대종단 맵, 일반: 모던 럭셔리) 적용.
3. **대형 와이드 히어로 카드 vs 컴팩트 카드 계층화**:
   - `docs/ux-blueprint.md` 우선순위(진행 중 ✈️ $\rightarrow$ D-Day 임박)에 따라 1순위 대표 여정을 **대형 와이드 히어로 카드**로 부각.
   - 히어로 카드에 주요 여행 코스 스트립(`리마 ➔ 쿠스코 ➔ 우유니 ...`) 탑재.
   - 완주한 지난 여행 및 기타 여정은 공간 효율적인 **컴팩트 카드**로 정돈.

## [v1.2.4] - 2026-09-07
### AI 초안 검토와 원본 보호 오케스트레이션
1. 일정 생성, 여행기 윤문, 영수증 OCR 결과를 원본과 분리된 검토 화면에서 확인한 후 적용하도록 변경.
2. 기존 일정은 유지하고 시간·제목이 중복되지 않는 승인 항목만 병합. 여행기 원문은 `originalText`로 보존.
3. revision 비교, 작업 ID 멱등 처리, 취소·시간초과·중복 승인·저장 실패 복귀를 담당하는 UI 독립 오케스트레이터 추가.
4. `st_trips_v2` 접근을 저장소 어댑터로 감싸고 손상 원본 별도 백업, 저장 오류 전달 및 초안 원자 적용 계약 추가.
5. OCR 실패 시 임의 예제 지출 자동 입력을 제거하고 사용자가 직접 입력할 수 있도록 기존 폼 유지.
6. 아키텍처, 오케스트레이션, UI/UX 및 단계별 개발 모델 문서와 저장소·오케스트레이터·앱 셸 자동 계약 테스트 구축.
7. Edge `--dump-dom` 출력 의존성을 제거하고 임시 CDP 세션으로 앱 초기화·버전·런타임 오류를 확인하도록 사전 검증기 보강.

## [v1.2.3] - 2026-09-06
### 🌐 평생 세계 및 대종단 등록된 모든 여행 궤적 통합 표기 엔진
술술 오도미터 순차 올림 원칙(`v1.2.2` $\rightarrow$ `v1.2.3`)에 따른 사용자 피드백 대응 릴리즈.
1. **평생 여행 발자취 내 '세계 및 대종단 비행 궤적 노선도' 전 여행 통합 표기**:
   - 통계 대시보드의 **[평생 여행 발자취]** 탭에서 `세계 및 대종단 비행 궤적 노선도`의 `[지도 열기]` 클릭 시, 단일 여행만 표시되던 문제를 해결하고 등록된 모든 여행(`State.trips` - 중국 운남, 남미 4개국 등)의 전 세계 비행 궤적과 거점 핀을 멀티 컬러 팔레트로 하나의 Leaflet 인터랙티브 지도 위에 동시 시각화(`openGlobalRouteMapModal`).
2. **다중 여정 멀티 컬러 팔레트 & 전 세계 비행 궤적 시각화**:
   - 각 여행별 고유 시그니처 색상 부여: 남미(골드/앰버 `#f59e0b`), 중국 운남(에메랄드/민트 `#10b981`), 유럽/기타(스카이블루 `#0284c7`, 로즈, 퍼플 등).
   - 각 여행의 비행 궤적 점선 및 번호 핀을 전 세계 지도 위에 동시 렌더링하고, 아시아부터 남미까지 모든 좌표를 포괄하는 자동 전 세계 카메라 피팅(`fitBounds`) 구현.
   - 각 거점 핀 클릭 시 `[이 여정 열기]` 및 `[구글 지도 길찾기]` 버튼 제공.
3. **글로벌 통합 메트로 관광 일러스트 노선도 엔진 (`renderGlobalIllustratedRouteMap`)**:
   - 노선도 탭에서도 등록된 모든 여행의 거점을 순차 나열하고, 대륙 간 비행 환승 브릿지(`✈️ [중국 운남] ➔ [남미 4개국] 대륙 간 비행로 연결`)를 시각적으로 연결하여 평생의 여정을 한눈에 파악 가능.
4. **하단 대륙/여정 퀵 링크 바 및 원클릭 줌인 네비게이션**:
   - 하단 퀵 바에 `[🌐 전 세계 궤적]` 및 각 여행별 `[🇨🇳 중국 운남 7개 거점]`, `[🌎 남미 9개 거점]` 원클릭 줌인 버튼을 탑재하여 대륙 간 순간 이동 탐색 지원.
5. **워크스페이스 메인 지도 전체 궤적 토글 스위치 (`btn-toggle-all-trips-map`)**:
   - 워크스페이스 내 인터랙티브 지도 툴바에 `[🌐 전체 여행 궤적]` 토글 버튼 신설.
   - 단일 여행 집중 탐색 모드와 나의 평생 전 여정 동시 오버레이 모드를 원클릭으로 손쉽게 전환 가능.

## [v1.2.2] - 2026-09-06
### 🧭 여행별 인터랙티브 지도(Leaflet) 100% 매칭 & 상단 지구본 버튼 직접 연결 쇄신
술술 오도미터 순차 올림 원칙(`v1.2.1` $\rightarrow$ `v1.2.2`)에 따른 사용자 피드백 대응 릴리즈.
1. **상단 헤더 지구본 버튼(`[🌎]`) 인터랙티브 지도 직접 연결**:
   - 기존에 정적 이미지 팝업(`modal-route-map`)으로 하드코딩 연결되어 있던 헤더의 지도 버튼을 전용 네비게이션 함수(`navigateToInteractiveMap`)로 전환.
   - 클릭 시 워크스페이스 메인 `일정/지도` 탭 및 `인터랙티브 지도` 서브탭으로 즉각 전환 후 Leaflet 지도 컨테이너로 부드럽게 스크롤 이동(`scrollIntoView`) 및 사이즈 무결성 갱신(`invalidateSize`).
2. **중국 운남 등 여행별 지도 헤더 & 거점 카드 완전 동기화**:
   - 인터랙티브 지도 상단 헤더에 고정되어 있던 '남미 대종단' 텍스트를 제거하고, 여행지 플러그인 팩(DKP)과 연동하여 `🇨🇳 중국 운남 13일 황금 코스 인터랙티브 궤적` 또는 `🌎 남미 4개국 22일 대종단`으로 실시간 맞춤 렌더링.
   - 여행 전환 시 하단 상세 도시 카드(`#card-dynamic-city`)를 해당 여행지의 첫 번째 거점(중국 운남: 쿤밍, 남미: 쿠스코/LA)으로 자동 리셋하여 타 여행지 데이터 간섭 원천 배제.
3. **여행지별 지능형 아이콘 및 툴팁 체계 확립**:
   - 중국/아시아 여행: `fa-earth-asia text-emerald-500`
   - 미주/남미 여행: `fa-earth-americas text-amber-500`
   - 기타 전역: `fa-map-location-dot text-sky-500`
   - 툴팁 역시 `${trip.title} 인터랙티브 지도 보기`로 동적 적응.
4. **허브(Hub) 카드 및 통계 모달 지도 바로가기 통합**:
   - 허브의 각 여행 카드 우측 상단 및 하단 지도 버튼 클릭 시 해당 여행 워크스페이스를 열고 즉시 인터랙티브 지도로 진입하도록 통합.

## [v1.2.1] - 2026-09-06
### 📱 모바일 UI 오버플로우 방어, 핑크색 시인성 쇄신, 동적 관광 일러스트 노선도 & Gemini AI 연동 고도화
술술 오도미터 순차 올림 원칙(`v1.2.0` $\rightarrow$ `v1.2.1`)에 따른 사용자 피드백 긴급 대응 및 핵심 경험 혁신 릴리즈.
1. **모바일 뷰포트 오버플로우 및 줄바꿈 결함 원천 방어**:
   - 모의 GPS 테스트 드롭다운(`#select-sim-gps`)에 `w-full sm:w-auto max-w-full truncate min-w-0`를 적용하여 360~390px 화면에서 카드 경계를 뚫고 나가는 현상 완전 해결.
   - 4대 서브탭 바(`#plan-subtabs-nav`)에 `overflow-x-auto no-scrollbar` 및 버튼별 `shrink-0 whitespace-nowrap`을 적용하여 '8대 도시 가이드'가 3줄로 찌그러지던 현상 근절.
   - 글로벌 `.no-scrollbar` CSS 규칙을 명시하여 모바일 터치 스크롤 시 투박한 네이티브 회색 스크롤바 노출 원천 차단.
2. **핑크/라벤더 시인성 결함 쇄신 (WCAG AAA 7:1+ 초고대비 달성)**:
   - 거점 도시 가이드 고도 뱃지 및 지도 고도 표시가 라이트 테마에서 `text-purple-300`의 연한 색상으로 인해 글씨가 읽히지 않던 문제를 해결.
   - 라이트 모드에서는 `text-purple-950 bg-purple-100 border-purple-300 font-bold`, 다크 모드에서는 `dark:text-purple-300 dark:bg-purple-950/70`로 분기하여 주간 야외에서도 즉각 인지 가능한 최고 수준의 가독성 확보.
3. **동적 관광 일러스트 노선도(뱀 모양 메트로 스타일) 엔진 탑재**:
   - 단일 정적 이미지에 머물던 `modal-route-map`을 전 세계 모든 여행지를 지원하는 동적 3대 뷰어 체제로 전면 개편.
   - 사용자 제공 여행사 인포그래픽과 동일한 뱀 모양 메트로 스타일(Serpentine Metro Track) 여정 라인을 자동 생성 (`renderDynamicIllustratedRouteMap`).
   - 국가별 플래그 배너, 거점 도시 정거장, 핵심 랜드마크 아이콘, 이동 수단(✈️ 하늘길 항공 점선 vs 🚌 대지길 육로 실선) 시각화 및 원클릭 구글 지도/일정 바로가기 연동.
   - [🎨 관광 일러스트 노선도] / [🗺️ 인터랙티브 위성 궤적] / [🖼️ 원본 가이드 인포그래픽] 3대 탭 전환 지원.
   - AI 일정 생성(`generateAiItinerary`) 시 새로 생성된 동선이 노선도에 즉각 자동 반영.
4. **Gemini 2.0 Flash AI 연동 고도화 & 실시간 피드백 챗봇**:
   - 미지원 `gemini-2.5-flash` 지연 호출을 제거하고 검증된 `gemini-2.0-flash` 1차 호출 체계 확립.
   - API Key 파라미터 URL 인코딩(`encodeURIComponent`) 처리 및 Google API 에러(400 유효하지 않은 키, 429 무료 할당량 초과) 상세 파싱.
   - AI 채팅창 전송 즉시 '생각 중...' 스피너 표시 및 키 미입력/에러 발생 시 무의미한 오프라인 문자열 반복 대신 원클릭 [⚙️ API 키 등록] 버튼과 구체적인 해결 가이드 제시.
   - AI 채팅 모달 상단에 `🟢 Gemini 2.0 Flash 연동됨` / `🟠 오프라인 (키 미등록)` 실시간 상태 뱃지 장착.

## [v1.2.0] - 2026-09-06
### 🌎 실제 여행 검증 남미 3주 풀 코스(22일/21일) 120+ 스팟 탑재 & 스마트 자동 채우기
술술 오도미터 순차 올림 원칙(`v1.1.9` 자릿수 올림 $\rightarrow$ `v1.2.0`)에 따른 실전 여행 데이터 풀 패키지 탑재 릴리즈.
1. **사용자 실전 검증 남미 3주(22일/21일) 황금 여정 풀 스케줄 전면 탑재**:
   - 사용자가 직접 제작·검증한 `south_america_travel_plan.html` 실전 여행 일정(미국 LA 스탑오버, 페루 리마·이카·와카치나·쿠스코·성스러운 계곡·마추픽추, 아르헨티나 부에노스아이레스, 이과수 폭포 육로 30분 국경 통과, 브라질 리우데자네이루 등)의 30분 단위 정밀 타임라인 120여 개 스팟 전면 내장.
   - **22일 풀 코스(10/11 출발)**: `scheduleOct11Data` 기반 22일간의 완벽한 힐링/모험 황금 여정.
   - **21일 최적화 코스(10/12 출발)**: `scheduleOct12Data` 기반 시차 최적화 21일 코스 완벽 지원.
   - 각 스팟별 시간(`time`), 아이콘(`typeIcon`), 제목(`title`), 상세 설명(`desc`), 현지 실전 꿀팁(`tip`), 지출 통화(`curr`: USD, PEN, ARS, BRL, KRW), 카테고리(`cat`) 정밀 정규화.
2. **초기 샘플 가이드 빈 껍데기 버그 원천 해결 및 자가 치유(Self-Healing)**:
   - `kb-travel.js`에서 `saDays` 및 `saDays21`이 `window.KB_TRAVEL` 및 전역에 누락되어 빈 배열(`days: []`)로 로딩되던 초기화 결함을 완전 해결.
   - 기존에 생성된 빈 가이드 일정도 앱 실행 시 자동으로 22일 풀 코스 120+ 스팟으로 복구하는 Self-Healing 로직 구현.
3. **타임라인 스마트 제안 배너 & 원클릭 풀 코스 자동 채우기**:
   - 남미 여행 워크스페이스 진입 시, 일정이 비어있거나 초안 상태일 때 즉시 22일 풀 코스(10/11) 또는 21일 코스(10/12)를 덮어씌울 수 있는 스마트 배너 제공.
   - 환경설정 모달(`modal-settings`) 내 "22일 풀 예제 복원" 액션 지원.
   - DKP 남미 지식 팩(`pack-south-america.js`)의 `getGoldenItinerary(durationDays)`를 정밀 연동하여 21일/22일 황금 코스를 즉시 반환하도록 개선.
4. **불변 원칙 및 게이트 무결성 100% 통과**:
   - `tools-verify.ps1` 4대 게이트(필수 파일, 버전 동기화, R-1~R-10 불변 규칙, Microsoft Edge V8 런타임 검증) 100% PASS.

## [v1.1.9] - 2026-09-06
### 🎨 라이트 테마 팁 박스 초고대비(WCAG AAA 7:1+) 색감 쇄신 & 버전 뱃지 원클릭 릴리즈 히스토리 뷰어 탑재
술술 오도미터 순차 올림 원칙(`v1.1.8` → `v1.1.9`)에 따른 UI 시인성 극대화 및 사용자 친화적 버전 관리 릴리즈.
1. **라이트 테마 팁 박스 초고대비(WCAG AAA 7:1+) 색감 쇄신**:
   - 사용자 피드백(타임라인 스팟의 팁 박스에서 연노랑 배경 위 연노랑 텍스트로 인해 글자가 전혀 보이지 않던 결함)을 접수하여 전면 수정.
   - `.tip-box` 공통 클래스 및 고대비 규칙을 수립: 라이트 테마에서 배경 `#fef3c7`(연호박색), 테두리 `#fde68a`, 텍스트 `#78350f`(앰버 900, 대비 9:1 이상)로 강제 지정하여 실외 직사광선 아래에서도 100% 선명한 가독성 확보.
   - 타임라인 스팟 팁(`spot.tip`), 필수 예약 가이드 팁(`b.tip`), 특별 유의사항 타이틀(`sn.title`), 거점 도시 주의사항 팁(`d.caution`), 동적 서머리 스팟 팁(`s.tip`) 전체에 `.tip-box` 및 고대비 컬러 일괄 적용.
   - 다크 모드에서는 눈이 편안한 골드 톤(`text-amber-300`, `#fde68a`)과 은은한 반투명 앰버 배경(`rgba(245, 158, 11, 0.12)`)으로 자동 전환.
2. **버전 뱃지 원클릭 릴리즈 히스토리 뷰어 탑재 (`#modal-version-history`)**:
   - 상단 헤더의 버전 뱃지(`v1.1.9`) 클릭 시 전체 버전별 상세 변경 내역(Release Notes)을 직관적으로 열람할 수 있는 전용 모달 창 신설.
   - 버전 뱃지에 호버 효과(`cursor-pointer`, 보더/컬러 트랜지션) 및 툴팁(`title="버전 업데이트 기록 보기 (클릭)"`) 부여. 부모 요소(`switchView('hub')`)로의 이벤트 전파를 차단(`event.stopPropagation()`)하여 오작동 방지.
   - 환경설정 모달(`modal-settings`) 내 "7. 술술트래블 엔진 버전 & 릴리즈 히스토리" 카드를 추가하여 설정 창에서도 언제든 버전 기록을 열람 가능하도록 지원.
   - `v1.0.0`부터 `v1.1.9`까지의 모든 기능 추가, 아키텍처 개편, 버그 해결 내역을 담은 타임라인 UI 구현.
3. **불변 법칙 R-10 및 무결성 검증 100% 통과**:
   - 게이트키퍼 정규식(`id="app-ver-badge"[^>]*>v([^<]+)<`) 호환성 완벽 유지.
   - `window.SulsulTravel.openVersionHistoryModal` 전역 네임스페이스 노출 및 Headless Edge V8 무결성 검증 통과.

## [v1.1.8] - 2026-09-06
### 🧩 목적지 플러그인 지식 팩(DKP) 레지스트리 구축 & 모놀리스 코드 분리 아키텍처 개편
술술 오도미터 순차 올림 원칙(`v1.1.7` → `v1.1.8`)에 따른 개발 아키텍처 구조 혁신 릴리즈.
1. **목적지 지식 팩(Destination Knowledge Pack, DKP) 레지스트리 아키텍처 도입**:
   - 기존 `index.html` 단일 파일 내 하드코딩되어 있던 목적지별(남미, 운남 등) 데이터와 비즈니스 로직을 플러그인 형태의 독립 모듈로 완전 분리 (`js/destinations/` 디렉터리 신설).
   - `destination-registry.js`: 목적지 지식 팩 자동 매칭(`resolve(trip)`), 등록(`register(pack)`), 기본 팩 폴백(`setDefaultPack(pack)`)을 관장하는 중앙 레지스트리 엔진.
   - `pack-default.js`: 사용자 등록 여행지의 일자와 스팟 데이터를 실시간 분석하여 모의 GPS, 도시 요약, 필수 체크리스트를 자동 생성하는 범용 동적 폴백 팩.
   - `pack-south-america.js`: 16개 핵심 명소, 8대 도시, 12대 필수 예약, 항공/이동 경로 좌표를 캡슐화한 22일 남미 대종단 마스터 팩.
   - `pack-yunnan.js`: 11개 핵심 스팟, 5대 거점 도시, 알리페이/옥룡설산 특화 가이드, 12박 13일 황금 코스 자동 생성기를 내장한 중국 운남성 특화 팩.
2. **모놀리스 `index.html` 코드 다이어트 및 DKP 전역 연동**:
   - 지도 엔진(`initOrUpdateLeafletMap`, `focusCityOnMap`, `resetMapToWorldView`): 목적지별 하드코딩 분기(`isSA`, `isYN`)를 제거하고 `resolveTripPack(trip)` 기반 단일 파이프라인으로 통합.
   - GPS 레이더(`populateSimGpsOptions`, `renderGpsRadar`, `simulateGpsLocation`, `updateGpsRadarUI`): 레거시 `GPS_KNOWN_SPOTS`, `GPS_DEMO_PRESETS` 상수를 제거하고 활성 팩의 `geo.simPresets` 및 `geo.knownSpots`로부터 실시간 쿼리.
   - 신규 여행 생성(`submitCreateTrip`): DKP `getGoldenItinerary()` 및 `pack.geo.destinations`와 자동 연동되어 새로운 목적지 추가 시 한 줄의 메인 코드 수정 없이도 완벽 지원.
3. **No-Build / Zero-Backend & R-10 불변 법칙 100% 계승**:
   - `npm` 빌드 도구 없이 브라우저 로컬 더블클릭(`file:///`)과 `tools-serve.ps1`, GitHub Pages 어디서나 즉시 구동.
   - 불변 법칙 R-10의 27대 필수 DOM ID 보존 및 Service Worker(`st-shell-v1.1.8`) 오프라인 프리캐시 자산 등록 완료.

## [v1.1.7] - 2026-09-06
### 🌏 신규 여행(중국운남여행 등) 내 남미 레거시 데이터 침범 결함 원천 해결 & 워크스페이스 전역 완전 동적화
술술 오도미터 순차 올림 원칙(`v1.1.6` → `v1.1.7`)에 따른 멀티 트립 아키텍처 전역 동적화 릴리즈.
1. **신규 여행 생성 시 남미 레거시 데이터 누출(Leak) 원천 차단**:
   - 기존 프로토타입 시절 하드코딩되었던 남미(페루 쿠스코, 마추픽추, 파타고니아 등) 데이터가 신규 여행(예: 중국 운남성 등 비(非)남미 여행) 워크스페이스에 무조건 렌더링되던 아키텍처 결함 완전 해결.
   - 여행 목적지 분류기(`isSouthAmericaTrip`, `isYunnanTrip`)를 구축하여 목적지별 특화 모드와 일반 여행 동적 모드로 안전 분기.
2. **GPS 레이더(R-10 `#gps-radar-card`) 목적지 인식 동적화**:
   - 운남 여행 진입 시 "추천 기본 모의 위치 (쿠스코 광장)" 대신 "🇨🇳 추천 기본 모의 위치 (쿤밍 취호공원)" 및 운남 11대 핵심 스팟(취호공원, 석림, 리장고성, 옥룡설산, 호도협, 송찬림사 등)으로 시뮬레이션 프리셋 및 거리 계산 엔진 완전 교체.
   - 불변 법칙 R-10의 27대 필수 DOM ID(`gps-radar-card`, `select-sim-gps`)를 100% 보존하면서 쿠스코/페루 정보 누출 0건 보장.
3. **워크스페이스 3·4번 서브탭 목적지 동적 쇄신**:
   - 3번 탭: 남미 여행 시 `[12대 필수 예약]`, 비남미 여행 시 `[여행 필수 체크/예약]`으로 동적 전환. 운남 여행의 경우 알리페이/위챗페이 투어패스, 옥룡설산 빙천 대삭도 조기 추첨, 고속열차 예매 등 맞춤형 꿀팁 자동 제공.
   - 4번 탭: 남미 여행 시 `[8대 도시 가이드]`, 운남 여행 시 `[운남 5대 도시 가이드]`(쿤밍, 따리, 리장, 옥룡/호도협, 샹그릴라), 일반 여행 시 `[여행지 스팟 모아보기]`로 자동 전환.
4. **리플릿 지도 뷰어(Subtab 2) 목적지 중심 자동 동기화**:
   - 남미 좌표 `[-15.0, -65.0]` 대신 운남 여행 시 운남 중심 좌표 `[26.2, 100.5]`로 자동 센터링 및 줌 배율 최적화.
   - 상단 빠른 도시 바로가기 버튼(`kunming`, `dali`, `lijiang`, `shangrila`) 및 랜드마크 핀, 여행 경로선 동적 렌더링.
   - 도시 상세 카드 또한 알파카 스테이크/고산병 대신 운남 과교미선, 차마고도, 리장 고성 바인딩.
5. **중국 운남성 12박 13일 황금 코스 자동 생성 & 스마트 원클릭 배너**:
   - 쿤밍(취호/석림) → 대리(고성/얼하이호) → 여강(고성/옥룡설산/인상여강) → 호도협(상호도협 트레킹) → 샹그릴라(송찬림사/두커종) 완벽 코스 지원.
   - 30분 단위 상세 타임라인, 현지 소요 비용(CNY 위안화 및 실시간 환율 보정), 테마별 팁 자동 생성.
   - 빈 일정으로 생성된 운남 여행 진입 시 상단에 `[✨ 운남 12박 13일 추천 코스 한 번에 채우기]` 스마트 배너를 제공하여 원클릭 세팅 완결.

## [v1.1.6] - 2026-09-06
### 📱 모바일 1화면 최적화 통합 스마트 필터 허브 쇄신 (스크롤 0px & 잘림 현상 원천 해결)
술술 오도미터 순차 올림 원칙(`v1.1.5` → `v1.1.6`)에 따른 UI/UX 모바일 최적화 릴리즈.
1. **상태 필터 5대 탭 세그먼트 컨트롤 1줄 통합**:
   - 기존 `overflow-x-auto`로 인해 모바일(360px~390px) 화면에서 우측 `[버킷리스트]`가 잘리고 가로 스크롤바가 발생하던 결함 완전 해결.
   - iOS 스타일의 `grid grid-cols-5` 균등 5분할 세그먼트 컨트롤로 개편하여 `[전체] [진행] [예정] [완료] [버킷]`이 1줄에 100% 온전히 표시되도록 쇄신.
2. **검색 & 정밀 필터 단일 카드 허브 통합**:
   - 2단 분리되어 있던 상태 필터와 검색 카드를 단일 일체형 카드 구조로 통합.
   - 검색창 우측에 `[초기화 ↻]` 및 `[격자/목록 ▦]` 토글 버튼을 콤팩트하게 통합 배치.
   - 하단 3대 드롭다운(`[전체 국가 ▾]`, `[전체 컨셉 ▾]`, `[최신 등록순 ▾]`)을 `grid grid-cols-3` 3컬럼 그리드로 배치하여, 이전의 가로 스크롤바 및 잘림 현상을 0px로 완벽 해소.

## [v1.1.5] - 2026-09-06
### 🔒 개인 Gemini API 키 보안 잠금·비활성화 UX 쇄신 및 안드로이드 PWA WebAPK 아이콘 고유 파일명(`v115`) 배포
술술 오도미터 순차 올림 원칙(`v1.1.4` → `v1.1.5`)에 따른 정밀 보안·UI 개선 릴리즈.
1. **API 키 보안 프라이버시 전면 쇄신 (개인 키 노출 원천 차단)**:
   - 키 저장 시 즉시 `[✅ 등록 완료 (보안 잠금)]` 상태로 전환되며 입력창을 `disabled` 및 `readOnly`로 비활성화.
   - 텍스트를 마스킹(`••••••••••••••••••••••••`) 처리하고, 눈동자(표시/숨김) 버튼을 숨기며 자물쇠 아이콘(`🔒`) 고정.
   - 키 접두/접미사(예: `AQ.Ab8••••Gb5g`) 노출을 전면 폐기하고 `기기 보안 저장소 보관 중 (총 XX자 등록됨)` 텍스트로 대체하여 제3자의 엿보기/복사 위험 원천 차단.
   - `[키 변경]` 토글 액션을 통해 의도적인 수정 시에만 잠금 해제되며, `[취소]` 및 `[삭제]` 기능 제공.
   - `[⚡ 연결 테스트]`는 안전하게 보관된 키를 바탕으로 백그라운드에서 키 노출 없이 원터치 검증 지원.
2. **안드로이드 PWA WebAPK 아이콘 고정 문제 원인 규명 및 고유 파일명(`-v115`) 배포**:
   - 기존 설치된 Android PWA(WebAPK)는 앱 설치 시점의 APK 패키지 내부에 아이콘과 스플래시가 하드코딩되므로, 기존 앱 삭제 후 재설치해야 즉시 곰돌이 아이콘이 반영됨을 상세 가이드화.
   - Google WebAPK 민팅 서버의 쿼리스트링 무시 및 캐싱 이슈를 극복하기 위해 `icon-192-v115.png`, `icon-512-v115.png` 등 물리적 고유 파일명을 생성하고 `manifest.webmanifest`에 쿼리스트링 없는 순수 경로로 연결.
   - 보조 작업공간(`여행준비`) 내 누락되었던 `icons/` 디렉터리 및 루트 파비콘/아이콘 풀세트 동기화.
   - 이미 설치된 PWA 단독 앱 환경에서는 설정 모달 내 버튼이 `[✅ 설치됨]`으로 자동 변경되도록 UX 정밀화.

## [v1.1.4] - 2026-09-06
### 🎨 술술실록 & 레슨런드 기반 텍스트 가독성·초고대비(WCAG AAA) 쇄신 및 테마 버튼 텍스트 증발 결함 원천 해결
술술 오도미터 순차 올림 원칙(`v1.1.3` → `v1.1.4`) 및 술술다이어리 실록/레슨런드 헌법에 따라, 모바일 환경에서 발생하던 테마 전환 버튼 텍스트 증발(블랙 버튼 위 블랙 텍스트) 결함을 원천 수정하고, 골드 버튼 및 카드 텍스트의 시인성을 WCAG AAA 초고대비(대비 9:1 이상)로 전면 쇄신한 릴리즈.

#### 1. 🐞 테마 버튼 텍스트 증발 결함 원천 해결
- **원인 규명**: CSS 내 전역 `.text-white` 오버라이드 셀렉터가 라이트 모드에서 `bg-[#1d1d1f]` 활성 버튼의 텍스트 색상을 `var(--ink)`(검정)으로 강제 덮어씌워, 검은 버튼 위 검은 글자(1:1 대비)로 렌더링되어 "라이트 그레이" 글자가 완전히 보이지 않던 결함 해결.
- **조치**: 다크 배경 요소(`bg-[#111827]`, `bg-[#1d1d1f]` 등)에 대한 화이트 텍스트 보호 규칙을 수립하고, `updateThemeUi()`에서 DOM 요소에 직접 `style.color = '#ffffff'`와 `style.backgroundColor = '#111827'`를 바인딩하여 100% 가시성 보장.

#### 2. ⚡ 골드 버튼 (`.btn-gold`) 시인성 대폭 개선 (WCAG AAA 달성)
- 기존 라이트 모드 골드 배경(`#d7a643`) 위 화이트 텍스트(대비 2.1:1, 실외에서 흐릿함)를 피치 블랙(`color: #0b0c0e !important; font-weight: 900 !important;`)으로 전면 전환.
- 샴페인 골드 배경과 칠흑색 텍스트의 **대비율 9.2:1 (WCAG AAA)**을 확보하여 강한 햇빛 아래 모바일 화면에서도 모든 버튼 텍스트(`현재 여행 원클릭 공유 링크 복사`, `+ 추가`, `새 여행 만들기` 등)가 선명하게 돋보이도록 개선.

#### 3. 🛡️ 서브텍스트 및 보조 버튼 고대비 최적화
- **토큰 대비 강화**: 라이트 모드 서브텍스트 `--ink2`를 `#555555`에서 고대비 `#374151`(Slate 700, 8:1 대비)로 승격하여 설명문 가독성 대폭 향상.
- **예제 관리 버튼 강화**: 연분홍/연베이지 파스텔 톤의 [남미 22일 예제 삭제]와 [예제 다시 불러오기] 버튼을 `bg-rose-100 text-rose-900 font-black` 및 `bg-amber-100 text-amber-950 font-black`으로 변경하여 시인성 확보.

## [v1.1.3] - 2026-09-06
### 📊 통계 전용 모달 창 분리, 최상단 브랜드 헤더 불변 고정 & PWA 아이콘 캐시 무효화
술술 오도미터 순차 올림 원칙(`v1.1.2` → `v1.1.3`)에 따라, 어느 화면(허브/워크스페이스) 및 모바일 해상도에서도 깨지지 않는 최상단 브랜드 헤더 블록을 확립하고, 상단 통계(📊)를 클릭 시 열리는 전용 모달 창(`modal-analytics`)으로 분리하여 메인 화면의 가독성을 극대화하였으며, PWA 홈 앱 아이콘 캐시 무효화 및 스마트폰 갱신 가이드를 완비한 릴리즈.

#### 1. 🌟 최상단 브랜드 헤더 블록 불변 유지 및 모바일 반응형 정돈
- **좌측 브랜드 블록 보호**: `[곰돌이 엠블럼]` + `Sulsul Travel` + `[v1.1.3 뱃지]`를 고정 앵커로 잠가, 360~390px 좁은 모바일 화면에서도 줄바꿈이나 찌그러짐 없이 완벽한 럭셔리 브랜드 아이덴티티 유지.
- **균형 잡힌 우측 4대 액션 클러스터**:
  - 허브 화면: `[📊 통계] [✨ AI비서] [🎛️ 설정] [+ 추가]`
  - 워크스페이스 화면: `[📊 통계] [✨ AI비서] [🎛️ 설정] [‹ 허브]`
  - 화면 전환 시 버튼이 불필요하게 늘어나지 않고 항상 4개로 유지되어 모바일 레이아웃 깨짐을 원천 방지.
- **워크스페이스 슬림 메타 바 신설**:
  - 개별 여행 제목, D-Day 뱃지, 일정 기간을 워크스페이스 상단 슬림 바로 우아하게 분리하고 `[경로 지도]`, `[동행 공유]` 퀵 버튼 배치.

#### 2. 📊 술술 통계 전용 모달 창 (`#modal-analytics`) 신설
- **통계 뷰 분리**: 허브와 워크스페이스 상단을 크게 차지하던 거대한 인라인 통계 카드를 걷어내고, 헤더 `📊` 버튼 터치 시 열리는 슬라이드업 모달 시트로 전면 개편.
- **듀얼 탭 대시보드**:
  - `[🌍 평생 여행 발자취]`: 총 여정, 방문 국가, 방문 도시, 여행 일수, 누적 지출, 기록 일기 6대 지표 + 대륙/비행 궤적 지도 연동 (`lifetime-analytics`).
  - `[✈️ 현재 여정 분석]`: 목표 예산 대비 소진율 프로그레스 바, 지출/예산, 방문 국가/도시, 다이어리/사진 현황 (`top-analytics`).
- **메인 화면 가독성 극대화**: 허브 진입 시 거대한 통계 블록 없이 곧바로 여행 카드 컬렉션이 시원하게 노출.
- **게이트키퍼 규격 보존**: 필수 DOM ID 27종(`lifetime-analytics`, `top-analytics` 등) 100% 온전히 유지.

#### 3. 🧸 PWA 홈 앱 아이콘 캐시 무효화 & 스마트폰 OS 갱신 가이드
- `index.html`, `manifest.webmanifest`, `sw.js`의 모든 아이콘 경로에 버전 쿼리 파라미터(`?v=1.1.3`) 부여.
- 서비스 워커 캐시를 `st-shell-v1.1.3`으로 갱신하고 마스커블 아이콘 사전 캐싱 목록 편입.
- PWA 설치 안내 모달 및 환경설정에 **"모바일 OS(iOS/Android) 특성상 기존에 홈 화면에 추가했던 아이콘은 정적 비트맵으로 고정되므로, 기존 아이콘 삭제 후 [홈 화면에 추가] 1회 재등록 시 새 곰돌이 아이콘이 즉시 반영된다"**는 친절한 비주얼 가이드 배너 탑재.

## [v1.1.2] - 2026-09-06
### 🔑 Gemini API 키 저장 신뢰도 확보 & 전역 토스트·실시간 연결 테스트 엔진 탑재
술술 오도미터 순차 올림 원칙(`v1.1.1` → `v1.1.2`)에 따라, API 키 저장 시 발생하던 전역 토스트 미정의 크래시를 완벽히 해결하고, 저장 피드백·실시간 상태 뱃지·마스킹 프리뷰·눈 표시 토글·실시간 연결 테스트를 탑재하여 사용자가 API 키 연동 상태를 100% 신뢰할 수 있도록 개선한 릴리즈.

#### 1. 🍞 전역 토스트 알림 엔진 (`showToast`) 공식 탑재
- **버그 해결**: `index.html` 내 25개 호출처에서 참조하던 `showToast()`가 선언되어 있지 않아 발생하던 `ReferenceError: showToast is not defined` 원천 해결.
- **다크/라이트 투톤 테마 반응형 토스트**: 최상단 z-[9999] 플로팅 알림창으로 모달 위에서도 선명하게 시각적 피드백 제공.
- 테마 전환, 데이터 백업/복원, API 키 저장, PWA 설치 가이드 등 전 영역의 사용자 액션에 즉각적인 인터랙티브 알림 제공.

#### 2. 🛡️ Gemini API 키 관리 UI/UX 대폭 강화
- **실시간 연동 상태 뱃지 (`#gemini-key-status-badge`)**:
  - API 키 등록 여부에 따라 `[✅ API 키 등록됨]`(초록) vs `[⚠️ 미등록 (오프라인 모드)]`(그레이) 실시간 시각화.
- **등록 키 마스킹 프리뷰 (`#gemini-key-preview-text`)**:
  - 저장된 키의 앞/뒤 자리와 총 자릿수(예: `AIzaSy••••4a2Z (39자)`)를 표시하여 키가 잘 보관되어 있는지 한눈에 확인 가능.
- **키 표시/숨김 눈 아이콘 토글 (`#btn-toggle-gemini-key`)**:
  - 비밀번호 마스킹(`type="password"`)과 일반 텍스트(`type="text"`)를 자유롭게 오가며 오탈자나 잘림 현상을 직접 육안으로 점검 가능.
- **저장 버튼 인터랙티브 피드백**:
  - `[저장]` 클릭 시 `[✅ 저장됨!]` 초록 애니메이션과 함께 즉각적인 성공 토스트 출력.
- **⚡ 실시간 Gemini 연결 테스트 버튼 (`#btn-test-gemini-key`)**:
  - Google Gemini 2.0 Flash 엔드포인트로 경량 핑(Ping)을 전송하여 발급받은 키의 유효성과 구글 API 서버 연결 상태를 즉석에서 판별.

#### 3. 🔄 데이터 무결성 및 인바리언트 규격 유지
- 27대 필수 DOM ID(`cfg-gemini-key`, `app-ver-badge` 등) 100% 보존 및 `modal-cfg-gemini-key`와의 양방향 동기화.
- `index.html`, `sw.js`, `kb-travel.js`, `CHANGELOG.md`, `술술트래블신록.md` 5개 파일 오도미터 순차 올림 완벽 일치.

## [v1.1.1] - 2026-09-06
### 🧸 공식 앱 아이콘 쇄신: 황금 날개 곰돌이 캐리어 & 술술 여행 마스코트 전면 탑재
술술다이어리 오도미터 순차 올림 원칙(`v1.1.0` → `v1.1.1`)에 따라, 기존의 획일적인 비행기/나침반 심볼을 걷어내고 사용자 제안 스타일을 100% 반영한 사랑스러운 **"황금 날개 달린 곰돌이 여행 캐리어 마스코트"**로 앱 공식 아이콘 및 PWA 에셋을 전면 쇄신한 릴리즈.

#### 1. 🧸 공식 브랜드 마스코트 캐릭터 아이콘 탄생
- **캐릭터 아이덴티티**:
  - 여행의 설렘을 상징하는 **황금 천사 날개**와 사랑스러운 갈색 곰돌이 형태의 **여행 캐리어/백팩 트롤리**.
  - 배 부분에는 술술트래블의 정체성을 담은 **술(酒) 도자기 병**, 낭만적인 **낚싯대**, 그리고 기차 여행을 뜻하는 **지하철/열차** 와펜 스티커 장착.
  - 배경은 은하수가 수놓아진 신비로운 우주와 푸른 지구 지평선으로, 전 세계 모든 여정을 안내하는 앱의 정체성을 담았습니다.
- **PWA 및 브라우저 파비콘 전 영역 동기화**:
  - `icons/icon-512.png` & `icons/icon-maskable-512.png` (512x512 고화질)
  - `icons/icon-192.png` & `icons/icon-maskable-192.png` (192x192)
  - `icons/apple-touch-icon.png` (iOS Safari 전용 192x192)
  - 브라우저 탭 `<link rel="icon" type="image/png" href="icons/icon-192.png">` 파비콘 연동.
- **앱 상단 헤더 로고 엠블럼 교체**:
  - 기존 폰트어썸 나침반 아이콘 대신, 곰돌이 캐리어 공식 엠블럼(`icons/icon-192.png`)을 상단 헤더 로고로 배치하여 앱의 브랜드 일체감 극대화.

#### 2. 🛡️ 술술 오도미터 순차 올림 규격 (`v1.1.0` → `v1.1.1`)
- `index.html`, `sw.js`, `kb-travel.js`, `CHANGELOG.md`, `술술트래블신록.md` 5개 소스 100% 동기화.

## [v1.1.0] - 2026-09-06
### 🔑 Google Gemini AI 무료 API 키 발급 가이드 & AI Studio 원클릭 연동 탑재
술술다이어리 오도미터 자릿수 올림 원칙(`v1.0.9` → `v1.1.0`, 뒷자리 9 도달 시 앞자리 +1 올림)에 따라, 사용자가 신용카드 등록 없이 1분 만에 평생 무료로 Gemini API 키를 발급받을 수 있도록 직관적인 4단계 비주얼 가이드와 Google AI Studio 원클릭 바로가기를 환경설정창 및 워크스페이스에 전면 탑재한 마이너 판올림 릴리즈.

#### 1. 🔑 Google Gemini API 무료 발급 가이드 및 원클릭 바로가기 지원
- **Google AI Studio 원클릭 바로가기**:
  - 환경설정 모달(`#modal-settings`) 내에 `[Google AI Studio에서 무료 API 키 발급받기 (새 창)]` 버튼을 장착하여 터치 한 번으로 공식 발급 콘솔(`aistudio.google.com/app/apikey`)에 즉시 연결.
- **1분 만에 끝나는 초간단 4단계 발급 안내**:
  1. **구글 로그인**: 신용카드/결제 정보 등록 없이 일반 개인 Google 계정으로 100% 평생 무료 이용 가능.
  2. **키 생성**: 상단 `[Create API key]` 클릭.
  3. **키 복사**: 생성된 `AIzaSy...` 키 복사.
  4. **술술트래블 등록**: 키를 붙여넣고 `[저장]` 누르면 AI 30분 단위 일정 기획 및 포토 감성 윤문 기능 무제한 연동 완료.
- **보안 및 개인정보 보호 안내**:
  - 입력된 API 키는 외부 중앙 서버로 전송되지 않고 오직 사용자의 브라우저 로컬 저장소(`localStorage`)에만 안전하게 보관됨을 보증.
- **워크스페이스 연동 지원**:
  - 상세 워크스페이스의 [동기화/설정] 탭 내에도 `[무료 키 발급 (Google AI Studio)]` 버튼을 추가 배치하여 어디서든 편리하게 접근 가능.

#### 2. 🛡️ 술술 오도미터 자릿수 올림 규격 (`v1.0.9` → `v1.1.0`)
- Semantic Versioning 및 술술 릴리즈 헌장 규칙("제일 뒷자리 9 도달 시 앞자리 올림 리셋")에 따라 `v1.0.9` $\rightarrow$ `v1.1.0`으로 순차 전환.
- `index.html`, `sw.js`, `kb-travel.js`, `CHANGELOG.md`, `술술트래블신록.md` 5개 소스 100% 동기화 완료.

## [v1.0.9] - 2026-09-06
### 📱 모바일 뷰포트 헤더 슬림화 & PC-스마트폰 GitHub Gist 클라우드 동기화 엔진 구축
술술다이어리 오도미터 순차 올림 원칙(`v1.0.8` → `v1.0.9`)에 따라, 360px~393px 모바일 스마트폰 화면에서 발생하던 가로 스크롤 및 뷰포트 터짐 현상을 근본적으로 해결하고, PC와 스마트폰 간에 데이터를 자유롭게 주고받을 수 있는 GitHub Gist 2-way 동기화 엔진을 구축한 릴리즈.

#### 1. 📱 모바일 헤더 슬림화 & 4대 핵심 액션 버튼 정돈
- **모바일 뷰포트 헤더 슬림화**:
  - 우측 액션 클러스터를 모바일 최적 4대 필수 버튼인 **`[ 📊 통계 ]`**, **`[ ✨ AI비서 ]`**, **`[ 🎛️ 설정 ]`**, **`[ + 추가 ]`**로 정돈.
  - 테마 전환 버튼(`☀️/🌙`)과 PWA 구동 상태 표기를 `#modal-settings` 내부로 이관하여 헤더 가로 공간을 120px 이상 획기적으로 절약.
  - `updatePwaUiState()`가 헤더의 `#btn-native-install`을 동적으로 확장(`flex`)하여 발생하던 가로 터짐 결함을 원천 차단 (헤더 버튼은 상시 `hidden` 유지, 설정 모달의 `#settings-pwa-status` 뱃지로 상태 전환).
- **글로벌 비행 궤적 지도 인터랙티브 맵 분산 배치**:
  - 전역 헤더에 단독 버튼으로 노출되던 세계 지도 뷰어를 개별 여행 카드(리스트뷰/그리드뷰 양쪽의 `[🗺️ 경로 지도]` 버튼), 상세 워크스페이스 상단 대시보드, 그리고 환경설정 모달 내 메뉴로 합리적 재배치.
- **모바일 가로 오버플로우 방지 CSS 전역 적용**:
  - `html, body { overflow-x: hidden; width: 100%; max-width: 100vw; box-sizing: border-box; }` 스타일을 주입하여 스마트폰 환경에서 한 픽셀의 오차도 없이 좌우 밀림 현상 방지.

#### 2. ☁️ 하이브리드 듀얼 데이터 동기화 아키텍처 (Zero-Backend URL Share + GitHub Gist Sync)
- **동행자 공유 (Zero-Friction Companion Share)**:
  - 친구나 가족과 여행 일정을 함께 볼 때는 번거로운 GitHub 계정 가입이나 PAT 발급이 필요 없는 **100% 무설치 URL 압축 패킹 공유(`exportActiveTripShareLink`)**를 유지하여 카카오톡 링크 1번 터치로 상대방 기기에 즉각 여행 카드 복제.
- **내 PC ↔ 스마트폰 간 양방향 클라우드 동기화 (GitHub Gist Sync)**:
  - 사용자가 설정 모달(`modal-settings`)에 GitHub Personal Access Token (PAT)을 1회 등록해두면, Secret Gist를 통해 PC와 모바일 간에 100% 무료·무제한 양방향 데이터 백업/복원 지원.
  - `syncToGist()`: 현재 로컬의 모든 여행 데이터를 Secret Gist에 안전하게 암호화 업로드 (Gist ID 자동 생성 및 로컬 저장).
  - `syncFromGist()`: 원격 Gist의 최신 여행 데이터를 스마트폰이나 PC로 즉시 내려받아 로컬 스토어 갱신 및 뷰 리프레시.
  - 설정 모달 내 동기화 연결 상태 뱃지(`#gist-sync-status-badge`) 및 마지막 동기화 일시 표시.

#### 3. 🛡️ 술술 불변 법칙 (Invariant R-1 ~ R-15) 및 게이트키퍼 100% 통과
- `tools-verify.ps1`의 27개 필수 DOM ID (`app-ver-badge`, `btn-native-install`, `header-trip-name` 등) 무결성 100% 보존.
- Microsoft Edge V8 Headless 렌더링 런타임 에러 0건 검증 완료.

## [v1.0.8] - 2026-09-06
### 🎯 시인성 전면 쇄신 & Sulsul-Diary Two-Tone (라이트 그레이 & 딥 블랙) 테마 시스템 구축
술술다이어리 오도미터 순차 올림 원칙(`v1.0.7` → `v1.0.8`)에 따라, WCAG AA 4.5:1+ 고대비 시인성 표준을 100% 충족하도록 텍스트-배경 대비를 전면 쇄신하고, Sulsul-Diary 정통 2-Tone 컬러 테마(애플 포그 라이트 그레이 `#f5f5f7` 및 딥 오닉스 블랙 `#0b0c0e`) 엔진을 구축한 릴리즈.

### 🌟 신규 기능 및 개선 사항 (Added & Improved)
- **[UI/UX] Sulsul-Diary Two-Tone Theme System (라이트 그레이 & 딥 블랙) 엔진 구축**:
  - **Light Gray (Apple Fog Canvas)**: `--bg: #f5f5f7`, `--surface: #ffffff`, `--line: #d6d6d6`, `--ink: #1d1d1f` (Apple Space Black), `--ink2: #555555`, `--accent: #a2601b` (Whisky Gold).
  - **Deep Black (Deep Onyx Luxury)**: `--bg: #0b0c0e`, `--surface: #16181d`, `--line: rgba(255,255,255,0.08)`, `--ink: #ededed` (Off-white), `--ink2: #a1a1aa`, `--accent: #d4af37` (Champagne Gold).
  - `localStorage.getItem('st_theme')` 기반 영구 테마 보관 및 `<head>` 직행 스크립트로 깜빡임(FOUC) 100% 방지.
- **[헤더 & 설정] 원클릭 테마 토글 버튼 & 환경설정 테마 스위처 카드 탑재**:
  - 최상단 앱 헤더: `[v1.0.8]` 뱃지 옆에 `[☀️/🌙]` 원클릭 테마 전환 버튼(`#header-btn-theme`) 배치.
  - 설정창(`#modal-settings`): `[☀️ 라이트 그레이]` / `[🌙 딥 블랙]` 직관적 2구 선택 카드 구축.
  - 브라우저 상단 시스템바 `<meta name="theme-color">` 실시간 동기화.
- **[시인성 쇄신] WCAG AA/AAA 초고대비 UI 가독성 전면 개선**:
  - PWA 설치 안내 모달(`#modal-install-guide`): 어두운 그라디언트 내 다크 그레이 텍스트 및 노란색 폰트 시인성 실패 문제를 완벽 해소.
  - 고대비 전용 테마 배너(`.theme-banner`) 및 플랫폼 탭(`.tab-install-active` / `.tab-install-inactive`) 도입으로 명암비 최대 21:1 달성.
  - 상태 필터(`.hub-tab-active`), 네비게이션 탭(`.nav-tab-active`), 서브탭(`.subtab-active`)을 CSS 변수 기반 시맨틱 클래스로 일원화하여 두 톤 모두에서 선명한 텍스트 가독성 확보.
- **[무결성 검증] Gatekeeper 4단계 전 관문 통과 및 Headless Edge V8 런타임 무오류 달성**:
  - `tools-verify.ps1` 4개 게이트 및 27대 필수 DOM ID 100% 보존 통과.

## [v1.0.7] - 2026-09-06
### 🎯 술술다이어리 웜 크림 럭셔리 테마 이식 & 상단 헤더·필터·AI 여행 생성 및 서버리스 공유 아키텍처
술술다이어리 오도미터 순차 올림 원칙(`v1.0.6` → `v1.0.7`)에 따라, 술술다이어리의 웜 크림 아이보리 럭셔리 라이트 테마와 상단 레이아웃을 전격 이식하고 다차원 필터/검색, 컨셉 태그 및 버킷리스트 기반 AI 30분 맞춤 일정 자동 설계, 남미 예제 삭제/복원, 100% 서버리스 URL 데이터 패킹 공유 아키텍처를 완비한 릴리즈.

### 🌟 신규 기능 및 개선 사항 (Added & Improved)
- **[UI/UX] 술술다이어리 웜 크림 아이보리 에디토리얼 럭셔리 테마 전면 이식**:
  - 부드러운 웜 크림 캔버스(`#f7f6f2`), 순백 서피스(`#ffffff`), 섬세한 베이지 테두리(`#e8e2d5`), 딥 에스프레소 차콜 텍스트(`#1c1917`), 위스키 앰버 골드 시그니처 엑센트(`#a2601b`) 완비.
  - `Cormorant Garamond` 및 `Playfair Display` 세리프 타이포그래피 로고 장착.
- **[헤더 재배치] 사용자 요청 규격 최상단 헤더 액션 클러스터 재정렬**:
  - 좌측: `Sulsul Travel` 세리프 로고 + 인생 다이어리 서브타이틀.
  - 우측: `[v1.0.7]` 뱃지 $\rightarrow$ `[📊 통계]` $\rightarrow$ `[🗺️ 구글맵]` $\rightarrow$ `[🎛️ 설정]` $\rightarrow$ `[+ 추가]` 순서 완벽 정렬.
- **[필터 시스템] 대규모 여행 아카이브 확장을 위한 상태 탭 & 다차원 검색/필터 툴바**:
  - 상태 탭 바: `[전체 N]`, `[진행중 N]`, `[예정 N]`, `[완료 N]`, `[버킷리스트 N]` 실시간 카운팅 연동.
  - 정밀 필터 툴바: 실시간 텍스트 검색창(`#hub-search-input`), 방문 국가 드롭다운, 여행 컨셉 드롭다운, 정렬(최신/일정/예산) 및 필터 초기화 버튼.
  - 듀얼 보기 모드 전환: 카드 격자 뷰 $\leftrightarrow$ 술술다이어리 스타일 럭셔리 가로형 리스트 뷰 원클릭 전환.
- **[여행 생성 모달 고도화] 컨셉 멀티 칩, 버킷리스트 메모 및 AI 30분 맞춤 일정 자동 설계 탑재**:
  - 8대 여행 컨셉 멀티 칩 선택 UI (`🎒 배낭`, `👑 럭셔리`, `👨‍👩‍👧 가족`, `🍽️ 미식`, `💰 가성비`, `🏞️ 대자연`, `🏛️ 문화`, `🧘 힐링`).
  - 가고픈 여행지/버킷리스트 자유 메모창 제공.
  - AI 자동 설계 체크 시 여행 등록과 동시에 30분 단위 추천 스팟 코스 즉시 자동 생성 (`generateAutoSpotsForTrip`).
- **[설정창 신설] 남미 22일 가이드 예제 삭제/복원 & Google Gemini API 키 통합 관리**:
  - `[🗑️ 남미 22일 예제 가이드 삭제]`: 나만의 여행만 심플하게 관리할 수 있도록 초기 샘플 안전 제거.
  - `[✨ 남미 22일 예제 가이드 복원]`: 언제든 지식 베이스 원본 가이드로 22일 완성 샘플 즉각 복구.
- **[서버리스 아키텍처] Zero-Backend URL 압축 패킹 원클릭 동행 공유 (`exportTripAsShareUrl` / `checkUrlShareImportOnStartup`)**:
  - 외부 중앙 데이터베이스 서버 없이도 `LZ-String` 압축을 통해 단일 URL 해시(`#share=...`)로 여행 카드 전체를 패킹하여 카카오톡 전송 지원.
  - 수신자가 링크를 열면 브라우저 로컬 스토리지에 동일 여행 카드가 즉시 복제 및 등록.

---

## [v1.0.6] - 2026-09-06
### 🎯 술술다이어리 실록 & 레슨런드 기반 5대 핵심 아키텍처 개선 (API 키 정제, AI 타임아웃, 폼 Dirty 가드, 방향키 탐색, 빈상태 럭셔리 UX)
술술다이어리 오도미터 순차 올림 원칙(`v1.0.5` → `v1.0.6`)에 따라, 술술다이어리의 기술 실록(`술술실록.md`)과 축적된 교훈(`레슨런드_및_버전규칙.md`)을 면밀히 분석하여 모바일/데스크톱 크로스플랫폼 안정성을 극대화한 핵심 아키텍처 쇄신 릴리즈.

### 🌟 신규 기능 및 개선 사항 (Added & Improved)
- **[실록 4.3.9 차용] API 키 제로위드 공백 및 특수문자 전역 정제 (`cleanApiKey`)**:
  - 모바일 브라우저나 메신저에서 Gemini API 키 복사 시 눈에 보이지 않는 유령 문자(`\u200B`~`\u200D`, `\uFEFF`), 따옴표, 줄바꿈이 유입되어 발생하던 구글 AI `400 API_KEY_INVALID` 오류 원천 차단.
  - API 키 저장 시(`saveGeminiKey`), State 초기화 시, 텍스트/비전 API 호출 시 실시간 자동 정제 적용.
- **[실록 4.3.9 차용] AI 멀티 모델 빠른 전환 및 10초/12초 타임아웃 방어 (`AbortController`)**:
  - `fetch` 요청 시 무한 로딩 스피너 프리징을 방지하기 위해 `AbortController` 기반 모델당 10초(텍스트 생성) / 12초(비전 OCR) 하드 타임아웃 장착.
  - 구글 서버 지연 발생 시 즉시 요청을 중단하고 1초 만에 차순위 모델(`gemini-2.5-flash` → `gemini-2.0-flash` → `gemini-1.5-flash`)로 신속 폴백.
- **[실록 4.4.1 & 4.4.4 차용] 편집창 Dirty State 이탈 방어 (`isModalFormDirty` / `safeCloseModal`) & 350ms Scrim 연타 쓰로틀**:
  - 여행 생성, 스팟 등록, 가계부 지출 입력 중 사용자가 실수로 배경(Scrim)을 터치하거나 취소/ESC를 눌렀을 때 작성 중인 내용이 유실되지 않도록 확인 팝업(`confirm`) 발화.
  - 버튼 조작 중 미세한 터치 오차로 모달이 닫히는 현상을 차단하는 350ms 쿨다운 쓰로틀 가드 탑재.
- **[실록 4.4.4 차용] 데스크톱/태블릿 PC 키보드 좌우 방향키(`ArrowLeft` / `ArrowRight`) 탐색 지원**:
  - 입력창 포커스가 없는 상태에서 좌우 방향키 입력 시 22일 일차 다이어리를 매끄럽게 연속 탐색(Day 전환)하고 피드백 토스트 표시.
- **[실록 4.4.1 & 4.4.2 차용] 빈 상태(Empty State) 럭셔리 엠블럼 & Direct CTA 버튼 표준화**:
  - 여행 카드 목록 및 가계부 지출 목록이 0건일 때 썰렁한 1줄 텍스트 대신 다크 럭셔리 원형 엠블럼, 명확한 온보딩 가이드, 직관적 액션 버튼(`[+ 새 여행 만들기]`, `[✨ 샘플 복구]`, `[+ 첫 지출 등록하기]`, `[영수증 자동 스캔]`) 전면 강화.
  - 브라우저 로컬 스토리지 한도 초과 시 친절한 안내를 제공하는 스토리지 쿼터 방어 토스트 장착.

---

## [v1.0.5] - 2026-09-06
### 🎯 술술다이어리 기반 PWA 시작 시 원클릭 자동 설치 & 인앱 브라우저 탈출 및 다크 럭셔리 UI/UX 고도화
술술다이어리 오도미터 순차 올림 원칙(`v1.0.4` → `v1.0.5`)에 따라, 이미 검증된 술술다이어리의 PWA 설치 아키텍처와 다크 럭셔리(Dark Luxury) 톤앤매너를 술술트래블에 전격 이식한 메이저 UX 쇄신 릴리즈.

### 🌟 신규 기능 및 개선 사항 (Added & Improved)
- **앱 시작 시 스마트 PWA 설치 온보딩 (`checkStartupInstallOnboarding`)**:
  - 앱 첫 진입 시 미설치 사용자에게 자동으로 스마트 설치 모달을 띄워 브라우저 캐시 삭제 시 여행 기록 소실을 방지하고 즉각 설치를 유도.
  - Sulsul-Diary 호이스팅 감지 패턴(`isInstalled`, `isIOS`, `inApp`)을 완벽히 이식하여 TDZ 오류 방지.
- **원클릭 네이티브 설치 프롬프트 직결 (`triggerNativeInstall`)**:
  - Android 및 PC Chrome/Edge 환경에서 복잡한 수동 설명 대신 골드 액션 버튼(`[📲 지금 바로 홈 화면에 앱 설치하기]`) 원클릭으로 브라우저 공식 `beforeinstallprompt`를 즉각 실행.
  - 설치 완료 시 `✓ PWA 구동 중` 상태로 실시간 전환.
- **인앱 브라우저 감지 및 Android Chrome Intent 탈출 지원**:
  - 카카오톡, 네이버 앱, 인스타그램, 페이스북, 라인 등 PWA 설치가 차단되는 인앱 브라우저를 실시간 감지하여 경고 및 전용 탈출 링크 제공.
  - Android 사용자를 위한 1-클릭 크롬 브라우저 전환(`intent://...#Intent;scheme=https;package=com.android.chrome;end`) 및 주소 복사 버튼 탑재.
- **메인 허브 상단 스마트 웰컴 배너 (`#smart-welcome-banner`)**:
  - 미설치 시 허브 대시보드 상단에 샴페인 골드 그라데이션 배너 노출 (`💡 시작 가이드 | 홈 화면 설치 (저장소 영구 보호) | 📲 터치 한 번으로 홈 화면에 앱 설치하기 [앱 설치] [✕]`).
  - 설치 완료 시 또는 닫기 클릭 시 세션 스토리지 기반 자동 숨김 처리.
- **다크 럭셔리 (Dark Luxury) 톤앤매너 & 버튼 디자인 전면 고도화**:
  - 딥 옵시디언(`080b12`)과 샴페인 골드(`D4AF37`)를 결합한 `.btn-gold`, `.btn-surface`, `.pulse-gold` 입체 버튼 시스템 도입.
  - `#modal-install-guide`에 `🌟 WELCOME TO SULSUL TRAVEL` 럭셔리 헤더 배너 및 정밀 iOS 사파리 3단계 공유 SVG 아이콘 탑재.

---

## [v1.0.4] - 2026-09-06
### 🎯 3대 핵심 혁신 기능 전면 탑재: 실시간 GPS 스팟 레이더, Gemini Vision 영수증 OCR, 1-클릭 PDF 여행 보고서 & 포토북
술술다이어리 오도미터 순차 원칙(`v1.0.3` $\rightarrow$ `v1.0.4`)에 따라 로드맵 상의 3대 핵심 혁신 기능 전격 구현 릴리즈.

### 🌟 신규 기능 및 개선 사항 (Added & Improved)
- **오프라인 & 실시간 GPS 스팟 매칭 레이더 (`#gps-radar-card`)**:
  - `navigator.geolocation` API 및 하버사인(Haversine) 구면 삼각 거리 공식 연동.
  - 현재 GPS 위치 기준 가장 가까운 관광지/맛집/투어 스팟 실시간 탐색 및 거리/도보 소요 시간 계산 (예: `📍 180m 앞 (도보 2분)`).
  - 국내 및 실내에서도 남미 주요 거점(쿠스코 아르마스 광장, 마추픽추, 부에노스아이레스, 이과수, 리우 등)의 위치를 즉시 시뮬레이션할 수 있는 **남미 8대 거점 모의 GPS 시뮬레이터** 탑재.
  - [➕ 오늘 일정에 즉시 추가] 및 [🗺️ 구글 지도 길찾기] 원클릭 인터랙션 제공.
- **Gemini Vision 기반 영수증/티켓 사진 Multimodal OCR 자동 가계부**:
  - 다통화 가계부 및 지출 등록 모달에서 영수증, 비행기 티켓, 관광지 입장권 사진 촬영/업로드 지원.
  - HTML5 캔버스 이미지 정규화(최대 800px 최적화 압축) 및 Base64 인코딩.
  - `gemini-2.5-flash` $\rightarrow$ `gemini-2.0-flash` $\rightarrow$ `gemini-1.5-flash` 3-Tier Multimodal Vision Fallback 체인 구축.
  - 영수증에서 상호명, 카테고리, 결제 통화(`USD`, `KRW`, `ARS`, `PEN`, `CLP`, `BRL` 등), 결제 금액, 일자를 정밀 추출하여 가계부 입력 폼 자동 완성.
  - 오프라인 또는 API 키 미설정 시에도 즉시 체험 가능한 남미 스마트 모의 OCR Fallback 제공.
- **여행 완료 후 1-클릭 PDF 여행 보고서 및 포토북 내보내기 (`#modal-pdf-report`)**:
  - 워크스페이스 상단 및 허브에서 접근 가능한 인생 여행 완주 리포트 전용 모달 탑재.
  - 품격 있는 커버(여행명, 기간, 방문국 국기, 총 지출, 여행 모토), 종합 통계(카테고리별 지출, 아르헨티나 MEP 우대 환율 절감액), 일차별 타임라인 요약, 감성 일기 및 3:4 정규화 포토 갤러리 매거진 스타일 렌더링.
  - `@media print` 전용 A4 고해상도 인쇄 레이아웃 지원으로 브라우저 [인쇄 / PDF로 저장] 원클릭 출력.
  - 영구 보관용 독립형 HTML 보고서 파일 다운로드 기능 병행 제공.

---

## [v1.0.3] - 2026-09-06
### 💎 대규모 품질 및 UX 혁신: 모던 다크 럭셔리, Leaflet 드릴다운 지도, 안전 삭제 모달, 공통 클라우드 DB 연동
술술다이어리 오도미터 순차 원칙(`v1.0.2` $\rightarrow$ `v1.0.3`)에 따른 전면 품질 혁신 릴리즈.

### 🌟 신규 기능 및 개선 사항 (Added & Improved)
- **모던 다크 럭셔리(Modern Dark Luxury) 디자인 전면 쇄신**:
  - 딥 옵시디언(`080b12`) 캔버스, 세련된 서페이스 카드(`#0f1422`), 골드 앰버와 에메랄드 미세 액센트 적용.
  - 매끄러운 글래스모피즘(`backdrop-blur-xl bg-[#080b12]/90`) 적용 및 모바일 44px+ 터치 타깃 확보.
- **Pretendard 웹폰트 최신 CDN 연동 & 타이포그래피 무결성 보장**:
  - 100% 순수 UTF-8 인코딩 및 다중 시스템 폰트 폴백 스택 구축으로 텍스트 및 특수기호 깨짐 원천 방어.
  - 가독성을 극대화한 계층적 타이포그래피(제목, 메타, 본문 대비 강화).
- **파괴적 액션 방어: 2단계 안전 삭제 확인 모달 (`#modal-confirm-delete`)**:
  - 여행 카드, 스팟, 가계부 지출, 포토 삭제 시 즉시 지워지지 않고 대상명과 파괴 경고를 고지하는 전용 확인 모달 도입.
  - 사용자 실수에 의한 소중한 여행 기록 유실 원천 차단.
- **PWA 앱 설치 마스터 가이드 (`#modal-install-guide`) & 네이티브 설치 연동**:
  - iOS Safari(공유 $\rightarrow$ 홈 화면에 추가), Android Chrome/삼성인터넷, PC Edge/Chrome 3대 플랫폼별 시각 안내 탑재.
  - 브라우저 `beforeinstallprompt` 이벤트 연동으로 지원 기기에서 상단 [📲 앱 설치] 원클릭 버튼 자동 활성화.
- **동행자 실시간 공통 클라우드 DB 연동 & 룸코드(Room Code) 공유 (`#modal-share-room`)**:
  - 여행별 고유 룸코드(예: `SA-2026`) 발급 및 1클릭 카카오톡/초대 링크 복사 기능 구축.
  - 동행자가 링크 접속(`?room=...`) 시 해당 여행 다이어리에 즉시 자동 참여 및 `[👥 동행 공유 중]` 상태 연동.
- **Leaflet.js 기반 인터랙티브 세계 $\rightarrow$ 거점 도시 드릴다운 지도 (`#route-interactive-map`)**:
  - 세계 전체 비행 노선(인천 $\rightarrow$ LA $\rightarrow$ 리마 $\rightarrow$ 쿠스코 $\rightarrow$ 마추픽추 $\rightarrow$ 우유니 $\rightarrow$ 부에노스 $\rightarrow$ 이과수 $\rightarrow$ 리우) 골든 궤적 시각화.
  - 지도 마커 또는 상단 도시 알약 버튼 클릭 시 부드러운 카메라 줌인(`flyTo`) 및 거점 도시 스펙 인스펙터 연동.
  - 일정 내 모든 스팟 및 거점 도시에 **구글 지도(Google Maps) 실시간 길찾기 링크** 다이렉트 연동.

---

## [v1.0.2] - 2026-09-06
### 🚀 대규모 아키텍처 개편: 인생 여행 다이어리 & 멀티 트립 허브 (Lifetime Travel Hub)
단일 여행 뷰어에서 평생의 여행을 기록하고 관리하는 **"인생 여행 다이어리 (Lifetime Travel Diary)"** 멀티 트립 플랫폼으로 대대적 아키텍처 전환 완료 (술술 오도미터 버전 원칙에 따라 `v1.0.1` $\rightarrow$ `v1.0.2`로 순차 릴리즈).

### 🌟 신규 기능 (Added)
- **인생 여행 허브 대시보드 (`#view-hub`, `#lifetime-analytics`)**:
  - 누적 여행 수, 전 세계 방문 국가 수, 방문 도시 수, 평생 여행 총경비 실시간 집계.
  - 감성적인 트립 카드 그리드(`#trip-cards-grid`): 커버 이모지, 여행지, 일정(D-Day 또는 여행 완료), 총예산, 진행바, 태그 표시.
  - 간편한 트립 카드 삭제/관리 액션.
- **새 여행 만들기 (`#modal-create-trip`)**:
  - 여행 제목, 시작일/종료일, 기준 통화(7대 통화), 총 예산, 방문 국가 및 여행 스타일(힐링, 어드벤처, 문화탐방 등) 설정.
  - 등록 시 고유 ID 발급 및 인생 허브에 즉시 동기화.
- **AI 30분 단위 일정 자동 추천 & 신규 여행 연동**:
  - 신규 생성된 빈 여행 카드 진입 시 **Gemini AI 원클릭 추천 모달** 연동.
  - 설정된 국가·일정·스타일에 맞춰 일자별 30분 단위 추천 일정을 자동 생성하여 카드에 주입.
- **22일 남미 황금 대종단 마스터피스 맛보기 샘플 시드 (`trip_sa_showcase_22d`)**:
  - 최초 진입 사용자 및 모든 사용자가 즉시 경험할 수 있는 완성형 22일 남미 여행 쇼케이스 내장.
  - **22일 75개 이상의 30분~1시간 단위 초정밀 타임라인**: 10/11 LA 출발부터 페루(리마/이카/쿠스코/마추픽추), 볼리비아(라파스/우유니 2박3일), 칠레(아타카마), 아르헨티나(부에노스아이레스), 이과수 국립공원(아르헨/브라질 양측), 브라질(리우데자네이루)까지 전 구간 일정·소요경비·실전 팁 망라.
  - **12대 필수 예약 포털 다이렉트 가이드**: 마추픽추 공식 티켓, 잉카레일, 볼리비아 비자, 이과수 국립공원, 그리스도상 트레인 등 공식 사이트 바로가기 및 예약 팁 탑재.
  - **8대 핵심 도시 가이드**: 치안 등급, 환전 팁, 긴급 연락처, 필수 방문 스팟 안내.
  - **인터랙티브 동적 루트 맵**: 8개 거점 핀 클릭 시 해당 도시 정보 및 일자 바로가기 연결.
- **스토리지 스키마 업그레이드 (`st_trips_v2`)**:
  - 기존 `st_current_trip`에서 멀티 트립 구조로 데이터 무손실 자동 마이그레이션.
  - 쇼케이스 여행(`trip_sa_showcase_22d`) 불변 보존 회로 탑재.

---

## [v1.0.1] - 2026-09-06
### 🐛 긴급 버그 수정 (Critical Fixes)
- **모달(Bottom Sheet) 오버레이 레이어 뷰포트 점유 및 전체 클릭 먹통 버그 원천 해결**:
  - `modal-spot`, `modal-expense`, `modal-ai-trip`, `modal-ai-chat` 등 4개 모달 컨테이너가 닫힘 상태(`.sheet-closed`)일 때 `fixed inset-0 z-50` 영역 전체를 차지하여 하위의 모든 버튼("시간표", "전체 펼치기", "+ 스팟 추가", 탭 네비게이션) 클릭을 가로채던 결함 수정.
  - `.sheet-closed`에 `display: none !important; visibility: hidden !important; pointer-events: none !important;` 및 `transform: translateY(100vh)`를 부여하여 닫힘 상태에서 클릭 관통 및 오프스크린 완전 은폐 보장.
  - 데스크톱 중앙 정렬(`sm:items-center`) 시 모달 상단이 화면 하단에 삐져나오던 시각 결함 해결.
- **ESC 키 전역 모달 닫기 지원**:
  - 키보드 `Escape` 키 입력 시 열려 있는 모든 모달 시트를 즉시 안전하게 닫도록 이벤트 리스너 추가.
  - 모달 오픈 시 배경 스크롤 락(`overflow: hidden`), 닫힘 시 자동 해제 처리.
- **Day 일정 타임라인 "시간표" 버튼 인터랙션 강화**:
  - 일자별 "시간표" 버튼을 독립된 터치 타깃 버튼(`bg-[#1a233a] border border-[#283554] cursor-pointer`)으로 시각화 개선.
  - 일차 제목 클릭 시에도 타임라인 아코디언이 즉시 토글되도록 UX 향상.

---

## [v1.0.0] - 2026-09-06
### 🎉 공식 첫 런칭 (Initial Official Release)
**술술다이어리(Sulsul-Diary)**의 엔터프라이즈 프론트엔드 오케스트레이션 아키텍처를 차용한 100% 무서버(Zero-Backend), 오프라인 퍼스트(Offline-First) 여행 어시스턴트 & 데일리 다이어리 웹앱 공식 런칭.

### 🌟 신규 기능 (Added)
- **상단 실시간 종합 통계 대시보드 (Top Analytics Banner)**:
  - 총 예산 대비 지출액 및 소진율 실시간 프로그레스 바 (`#top-analytics`)
  - 여행 진행도 (D-Day 진행 바 및 일차 표시)
  - 방문 국가 & 도시 수 자동 집계
  - 데일리 다이어리 작성 편수 및 등록 사진 수 실시간 연동
- **30분 단위 스마트 일정 플래너 (Smart Itinerary Planner)**:
  - 10월 11일(22일 풀 플랜) & 10월 12일(21일 최적화 플랜) 남미 황금 코스 탑재
  - 일자별 30분~1시간 단위 타임라인 아코디언 (개별/전체 펼치기 지원)
  - 30분 단위 일정 수동 추가 및 삭제 기능
- **Gemini Multi-Model Fallback Engine**:
  - `gemini-2.5-flash` ➔ `gemini-2.0-flash` ➔ `gemini-1.5-flash` 3단계 자동 폴백 회로
  - 사용자 목적지·기간·테마 기반 30분 단위 추천 일정 자동 생성
  - 1:1 AI 여행 어시스턴트 실시간 챗봇 인터페이스
- **Smart Draft (`[초안]`) 시스템**:
  - AI가 추천한 일정 및 코스 항목에 `[초안]` 배지 자동 부여
  - 사용자가 확인 후 터치 한 번으로 확정(`isDraft: false`) 또는 자유로운 커스텀 수정
- **다통화 실시간 가계부 & 아르헨티나 MEP 우대 환율 스위치**:
  - 7대 통화 지원: KRW, USD, PEN(솔), ARS(페소), BRL(헤알), EUR, JPY
  - 외국인 관광객 신용카드 우대 환율(MEP) 원클릭 토글 스위치 탑재
  - 카테고리별(식비, 숙박, 교통, 투어, 쇼핑, 기타) 지출 내역 및 원화 자동 환산
- **3:4 캔버스 자동 정규화 포토 다이어리 & 감성 에세이 작가**:
  - 브라우저 Canvas API 기반 3:4 규격 리사이징 및 경량화(WebP/JPEG)로 로컬 스토리지 쿼터 방어
  - 키워드 기반 AI 감성 여행 에세이 자동 윤문 기능
- **스마트 패킹 & 여행 준비 체크리스트**:
  - 여권, ESTA, 고산병 약, 어댑터 등 남미 특화 체크리스트 내장 및 실시간 진행률 연동
- **내장 지식 베이스 (`kb-travel.js`)**:
  - LA 2일 + 페루(리마/이카/쿠스코/마추픽추) + 볼리비아(우유니) + 아르헨티나(부에노스아이레스) + 이과수 폭포 + 브라질(리우) 22일 풀 코스 시드 탑재

### 🏗️ 아키텍처 및 인프라 (Architecture)
- **Zero-Backend**: 외부 서버 없이 GitHub Pages 100% 무료 정적 웹 호스팅으로 배포
- **Offline-First PWA**:
  - `sw.js`: Stale-While-Revalidate 캐싱 전략 탑재
  - `manifest.webmanifest`: 192x192, 512x512, maskable, iOS apple-touch-icon 규격 충족
  - 로컬 스토리지 단일 진실 공급원(SSOT) 아키텍처
- **Dark Luxury Aesthetic**:
  - Deep Onyx (`#0b0f19`) 배경, Golden Amber 및 Emerald 포인트 컬러
  - OS 기본 3D 이모지를 배제하고 순수 벡터 스트로크 SVG 아이콘 시스템 적용

### ✅ 검증 (Verification)
- Headless Microsoft Edge V8 런타임 구동 검증 통과 (런타임 에러 0건)
- PWA 오프라인 캐시 및 홈 화면 설치 규격 통과
- GitHub Pages 원격 저장소(`https://github.com/ysparkbc37D/Sulsul-Travel.git`) 배포 완료
